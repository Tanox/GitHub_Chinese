# 2026-09-26 工作日志

## 上下文：仓库版本已大幅前进
- 上一轮（2026-09-25）我提交并推送的是 **v1.11.2**（commit 95b23d3）。今日核验真实版本已是 **v1.12.0**（HEAD 577f67a，src/version.js 实测 1.12.0）；期间外部会话推进 1.11.15→1.11.16→1.12.0，合入覆盖率/缺口看板（T22，新增 /coverage 路由）、采集重构（T36）等。
- 因此 2026-09-25.md 中「当前 v1.11.2 / 待 bump 1.11.3」等记录已过时；后续一律以 `src/version.js` / `git log` 实时为准。

## 本日任务：审查项目原型与组件库规范（仅审查，未改代码）
- 审查范围：docs/prototype.md（规范，v1.12.0，与真实一致 ✅）、docs/architecture.md（v1.9.43 滞后）、openspec/prototype.md（索引副本 v1.9.48 滞后）、prototype/assets/*.css（设计系统，头注释全停 1.9.24）、prototype/prototypes/index.html（唯一原型屏）、src/components/navItems.ts、src/app/overview/page.tsx。
- 核心发现：
  1. **组件库孤儿化（高）**：prototype/assets 的 10 个 CSS 模块定义 .card/.badge/.ds-table/.tabs/.toast/.config-panel/.float-btn/.gh-window/.mobile-frame/.perf-grid/.swatch/.component-card 等可复用组件，但 prototype/prototypes/ 仅有 index.html 一个文件，且该屏完全不用这些类——改用内联 <style> 的 .ct-*（.ct-card/.ct-tab/.ct-terms/.ct-badge…）重实现等价组件；HTML 中搜索上述设计系统类 0 命中。.config-panel/.float-btn/.gh-window/.mobile-frame/.perf-grid 实为 1.9.46「原型重定向为采集工具」前的旧翻译脚本原型遗留，当前采集工具原型零引用。
  2. **令牌纪律违规（高）**：index.html 内联 .ct-* 大量硬编码 hex（#0d1117/#c9d1d9/#2ea44f/#dafbe1/#1a7f37/#fff8c5/#3fb950），未走 --c-* token；assets 内 effects.css .code-block 也硬编码 #b4e1a5。
  3. **路由数 3 vs 4（中）**：navItems.ts 与 src/app/ 均为 4 路由（含 /coverage）；但 overview/page.tsx 指标卡仍写「3 个 / 采集控制台·项目概览·设计系统」、architecture.md §7 路由表缺 /coverage。
  4. **规范缺口（中）**：本会话先前为原型加的顶栏（右上角设置齿轮 .proto-icon-btn）+ 页脚（.proto-footer）未写入 docs/prototype.md §7 映射/章节。
  5. **版本头漂移（低-中）**：prototype/assets/*.css 全 1.9.24、architecture.md 1.9.43、openspec/prototype.md 1.9.48、navItems.ts 1.11.16；仅 docs/prototype.md 1.12.0 与真实一致。
  6. **响应式不足（低）**：prototype.responsive.css 仅 900px 断点调 .proto-main/.proto-title；.ct-grid-2（1fr 1fr）与 .ct-steps 无窄屏降级。
- 治理建议：①让 index.html 复用 .card/.tabs/.ds-table/.badge（删内联 .ct-* 重复）或将 .ct-* 收敛为 design-system 的采集工具主题层并让 examples.css 有宿主页；②原型/应用样式禁硬编码色值、一律 token（可加 stylelint）；③补「组件库使用规范」小节；④路由清单从 navItems.ts 派生避免人工漂移；⑤统一版本头策略。
- 未改动代码（仅审查）。未提交。建议优先修 P0：overview「3→4」+ architecture.md 补 /coverage。

## 合并 OpenSpec 索引入 docs（v1.12.1）
- 用户要求把 /openspec 文档并入 /docs。`openspec/*.md` 经核实全为「指向 docs/ 的索引/速览」，正文本体在 docs/（无独立权威内容）。
- 动作：① `docs/README.md` 重写为唯一文档入口，并入 openspec 的导航索引与「速览」摘要；② 删除 `openspec/` 目录；③ 修正活跃引用——README/project/architecture 目录树移除 `openspec/`、development.md 版本同步清单移除 `openspec/config.yaml` 与 `[openspec/project.md]` 链接、coding-style.md 同链接改指 `./project.md`；④ 版本 1.12.0→1.12.1（patch）同步 src/version.js、package.json、README 徽章、docs 五份头注释、CHANGELOG 新增 [1.12.1]。
- 踩坑：仓库文档为 CRLF，初版脚本 `\n` 正则全失配；目录树行带反引号、顿号 `、`(U+3001) 字面量在脚本失配，导致多次失败；最终用 CRLF 安全正则 + `replace_in_file` 精确子串修完。教训已写入 MEMORY 工具链陷阱。
- 剩 openspec 引用均为 CHANGELOG/PROGRESS 历史条目与 docs/README 的「已合并移除」说明，无活跃断链。未提交。

## 应用 UI 对齐原型（首页重构，v1.12.1）
- 用户「重构应用的UI，他和原型不符合」。精确比对 `prototype/prototypes/index.html` + `assets/prototype.base.css` 与应用后确认：**设计令牌其实一致**（`base.css` 的 `--brand:#2ea44f`/`--ink:#1c2128`/`--border:#d1d9e0`/`--canvas:#f6f8fa` 与原型 `--c-*` 完全对应；组件类名 1:1 对应仅少 `ct-` 前缀）。"不符合"在结构层。深色侧栏(Rail)是产品多页结构（原型单页不建模导航），属合理差异，未改。
- 重构（未提交，文件头→1.12.1、UI 条目入 CHANGELOG [1.12.1]）：① `CollectorConsole` 用原型四区块（h2+meta）包裹 steps/grid/preview/dashboard，加语义化 id；② `DataCenter` 补原型描述「两种采集入口，智能清洗后一键导出 JSON。」；③ `Shell` 新增页脚（对齐 `proto-footer`）；④ `layout.css` `grid-2` 列宽 `2fr/3fr→1fr 1fr`；⑤ 新增 `.section`/`.footer` 样式。ESLint 三个 TSX 文件 EXIT:0。
- **会话间隙漂移教训（本会话实测）**：本会话早前读到的 `2026-09-26.md` 与 `src/version.js=1.12.0` 已被外部推进——最终 `version.js=1.12.1`（已提交）、`openspec/` 已合并删除、早前的 `prototype.md`/`project.md` 编辑已由 commit `c15ab3a` 提交。我的 UI 改动落到 1.12.1 树后，必须重校文件头(1.12.0→1.12.1)与 CHANGELOG(UI 条目从 [1.12.0] 移到 [1.12.1])。再次印证「每次动手前实查 version.js + git status + git log + 真实文件内容，绝不轻信会话初快照」。

## 审查项目 + 清理冗余文件（用户「审查项目，清理冗余文件」）
- 派 code-explorer 子代理全仓排查：src/ 与根 .cjs **无零引用死文件**（此前清理彻底）。唯一"仅测试引用未接入运行时"的是 5 个词典审阅 .cjs（review-store/merge-into-dictionary/term-operations/history-diff/io-dictionary）——属有测试的数据层模块、等 UI 接入，**非死代码不删**。
- 已删 2 个遗留冗余文件（commit，3 files/32 deletions）：`docs/config.yaml`（OpenSpec 遗留配置，零消费者，仅 docs/README.md 一条链接）→ 同步移除 README 第 19 行失效链接；`docs/untranslated-terms.txt`（0 字节空数据文件，运行时可选读取可重建）。
- 未删项：`docs/code-review/`（贡献者流程文档，脱离索引但有效，保留）；prototype/（server.js 仍用，资源无死链）；build/（按设计被 git 跟踪）。
- 功能重叠（属重构非删文件，已上报未动）：`dictionaries/index.js::mergeAllDictionaries` 与 `utils/tools/dictionaryProcessor.js::mergeDictionaries` 都合并运行时 translationModule；两个同名 `dictionaryProcessor.js` 仅文件名撞车（职责不同）。
- 注意：本次 git status 出现一批**非我产生的并行改动**（CHANGELOG.md、public/css/*、src/components/*）——用户/外部流程 WIP，未触碰；我的提交仅含 3 文件清理。
