# 每日日志（2026-09-26）

## v1.11.16 文档全量同步（T22 收尾）
- 用户「继续更新所有项目文档」：补齐 T22（覆盖率看板）发布所需的活文档版本同步。
- `docs/PROGRESS.md` 完整同步至 1.11.16：横幅+版本表、页面路由数 3→4（补 `/coverage`）、§4 新增 4.9（T22 小节）、§8 变更记录补齐 1.11.1–1.11.16（原表断层于 1.10.2）、§9 Roadmap 修正「仍缺少覆盖率度量」过时表述。
- `CHANGELOG.md` 1.11.16 日期 2026-09-25→2026-09-26（与 PROGRESS 对齐）。
- **未改动** `openspec/*`、`docs/architecture.md`、`docs/project.md`、`docs/prototype.md`、`docs/coding-style.md`、`docs/README.md` 的版本横幅（仍 v1.9.43/v1.9.48）：这些是各参考文档自身最后编辑版本，按 `PROGRESS §7`「未编辑文档不批量刷写版本行」铁律保留，避免无意义 diff。源码/测试文件头（`collect-dict.cjs` 等）同理保留 1.11.15。
- 校验：`search_content` 全仓 1.11.15 仅剩 CHANGELOG 历史小节、TASKS 归档 T36 记录、PROGRESS §8 新增的 1.11.15 行、及未编辑源码/测试头注释——均属历史/正确保留。
- 未提交（按规约未经要求不 commit）。

## 合并进度文档（TASKS + PROGRESS）
- 用户要求合并 docs/TASKS.md 与 docs/PROGRESS.md。执行期间用户并行自行 bump 版本至 1.12.0 并重写 PROGRESS §5/§9，导致我这批 replace_in_file 因 old_str 不匹配多数失败（教训：并行编辑须先重读）。
- 最终由用户完成合并：版本 1.11.16→1.12.0（MINOR，docs 收口）同步至 src/version.js、package.json、README、PROGRESS 横幅、CHANGELOG 新增 [1.12.0] 小节；PROGRESS §5 改为活动任务清单（仅 T18/T21 未完成），§9 改为指向 §5；删除 docs/TASKS.md。
- 我补修一处遗漏：PROGRESS 版本表（§1.1 第 16 行）仍残留 v1.11.17，已改为 v1.12.0，全链路版本一致。
- 注意：存档的 PROGRESS §4/§8 中 `docs/TASKS.md` 引用均为历史变更事实（v1.9.33/1.9.34/1.9.41），保留不改；无活跃交叉引用断裂。
- 未提交。

## 原型规范同步（对齐应用代码）
- 用户「让应用代码和原型对齐」→ 选「同步规范文档」：将 `docs/prototype.md` 从 v1.9.48 同步至当前实现 v1.12.0（覆盖此前"未编辑不刷写"的保留态，属用户显式指令）。
- 改动（不 re-bump 版本，并入既有 WIP 1.12.0）：①头部横幅 1.9.48→1.12.0；②§1 导航三页→四页，补 `/coverage`（T22 覆盖率/缺口看板）；③§7 映射表补覆盖率看板落点行；④§10 版本历史补 1.12.0 行；⑤CHANGELOG [1.12.0] 补 prototype.md 同步说明。
- 未编造：T19–T25（审阅/合并/历史/导入导出/搜索）数据层脚本虽已实现，但 UI 接入与 localStorage 持久化仍 pending，原型/映射表未声称其已在前端落地。
- 未提交。
