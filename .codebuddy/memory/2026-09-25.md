# 2026-09-25

## 清理根目录冗余文件（用户「减少项目根目录下的文件数量，并去除冗余文件」）
- 根目录 17 个散落文件中，仅 2 个为**与项目交付物无关的 AI 工具产物**，已删除：
  - `GEMINI.md`（72B，Gemini 自定义指令「对话 Suggestions 保持中文」）
  - `metadata.json`（含 `MAJOR_CAPABILITY_SERVER_SIDE_GEMINI_API`，Gemini/AI Studio 项目元数据）
- 核验：全仓 `search_content` 搜 `metadata.json|GEMINI|MAJOR_CAPABILITY` 仅命中 metadata.json 自身；无 CLAUDE.md/AGENTS.md/.cursorrules 等其他 AI 指令文件；两文件均未被任何构建/脚本引用；均被 git 跟踪（来源 `9d981fa chore: improve tooling and dictionary collection`）。删除后根目录文件 17 → 15。
- 其余 15 个根文件均为必要：`build.cjs`/`collect-dict.cjs`（构建/词典）、`eslint.config.js`+`eslint/`（规范）、`next.config.mjs`/`postcss.config.mjs`/`tailwind.config.ts`/`tsconfig.json`/`next-env.d.ts`（Next/TS 配置）、`server.js`（dev:prototype）、`package.json`/`package-lock.json`、`README.md`/`CHANGELOG.md`/`CONTRIBUTING.md`/`LICENSE`。
- 主动提示但未删（属功能/资产配置，非"冗余文件"）：`prototype/`（26 个旧 HTML/CSS 原型，被 `dev:prototype`/`server.js` 使用，已被 Next 工作台取代，可未来清理）；`openspec/`（按既有约定仅作 docs/ 索引，保留）。
- 未提交（按规则未经用户要求不 commit）。

## 采集成功率 P1（T12–T14）实现 · v1.10.0
- 用户选择先推进 P1 采集成功率（T12 提取精准化 + T13 SPA/动态适配 + T14 单页鲁棒性）。
- 新增 `src/lib/page-navigation.js`（Node 侧、无浏览器依赖、可单测）：`gotoWithFallback`（networkidle2 超时降级 domcontentloaded+固定等待）、`waitForHydration`（#react-app）、`autoScroll`、`navigateWithRetry`（指数退避 1s/2s/4s、上限 3 次，识别 429/超时/网络错误）、`isRetryable`/`computeBackoffDelay`/`RetryableError`。
- `extract-page-text.js`（T12）：作用域从 `document.body` 收窄为 `resolveScopeRoot()`（#react-app → .application-main → body），并跳过 markdown-body/highlight/blob-code/CodeMirror/评论等「内容型容器」降噪。
- `collector-core.js`（T13/T14）：逐 URL `yield* navigateWithRetry` + `waitForHydration` + `autoScroll`；删去冗余常量与魔法数（429/150 抽常量）。
- 新增 `tests/page-navigation.test.mjs`（4 例）。`collector-core.js` 因超 200 行已把 `navigateWithRetry` 拆入 `page-navigation.js`，门禁 0 超限、lint 0 warning。
- 版本升 minor → **1.10.0**（`src/version.js`/`package.json`/README/CHANGELOG/PROGRESS/TASKS 同步）；`npm test` 全绿（24 用例、0 error/0 warning）。
- 注意：会话间隙 `version.js` 已被外部 bump 到 1.9.49 并随后提交为 1.10.0；当前 HEAD 已含代码/版本/测试，仅 `collector-core.js`、PROGRESS/TASKS/CHANGELOG、构建产物尚未提交。未经用户要求未 commit。

## 用户脚本文件名更名（用户「更新构建的脚本名称为 GitHub_zh-cn.user.js」）
- 将构建产物 `build/GitHub_i18n.user.js` 更名为 `build/GitHub_zh-cn.user.js`（仅改文件名，**仓库名 `GitHub_i18n` 与全局变量 `window.GitHub_i18n` 保持不变**）。
- 同步点：`build.cjs`（OUTPUT_FILE + `@updateURL`/`@downloadURL`）、`ci-cd.yml`（产物名/路径）、`src/config.js` 与 `src/versionChecker/fetcher.js`（更新检查 URL）、`src/lib/project-metrics.ts`、`src/app/overview/page.tsx`、测试/校验脚本；重新 `npm run build` 生成新产物并删旧产物（validate 通过）。
- 文档：README 命名说明改为新名并说明更名后果（旧安装无法自动更新，需重装）；`docs/`(project/PROGRESS/development/architecture) + `openspec/`(project/development/architecture 及 README/coding-style/prototype/config.yaml) 的脚本名引用与**版本横幅统一补同步到 v1.9.43**（外部 `cd8f9f9` 发布 v1.9.43 时同步不完整，漏了 project/development/architecture 横幅与 openspec）。
- 提交 `2c4ce96`（25 文件，+58/−54，git 识别为重命名 99%）。未 bump 版本（沿用外部已发布的 1.9.43；改名属配置/命名变更）。

## 纠正：GEMINI.md / metadata.json 不得清理（用户全局偏好）
- **2026-09-25 早些时候误删**了 `GEMINI.md` 与 `metadata.json`（误判为 Gemini 工具冗余产物）。用户明确：**这两个文件是 Google AI Studio 必需的，必须保留，任何「去冗余 / 减文件」任务都不得清理它们**。
- 已用 `git checkout HEAD -- GEMINI.md metadata.json` 恢复，内容校验一致（`GEMINI.md` 中文指令；`metadata.json` 含 `MAJOR_CAPABILITY_SERVER_SIDE_GEMINI_API`）。
- 已写入 `MEMORY.md`「受保护文件（勿清理 / 勿删除）」长期条目，后续会话自动跳过这两个路径。

## Next.js 代码审查整改（用户「审查项目 / 修复所有」，nextjs-code-review 技能）
- 用 nextjs-code-review 技能对 App Router 做专项审查，定位 W1–W3 + S1–S4（S5 为备注无需改码）。
- 关键前提：项目被外部高频 bump 至 **v1.10.0**（HEAD `8193f7f`，未提交本地改动含 `collector-core.js`/`build`/`docs`）。早先读到的旧 `collector-core.js`（含 `RAW_TERMS_FILE`）已过期；当前已抽出 `extract-page-text.js`、`page-navigation.js`，且**临时文件竞态在 v1.9.47 已修复**（`createRawTermsPath`+清理），故审查中 W3「竞态」子项已无需处理。
- 已落地修复（均通过 tsc/lint/lint:length/单元测试/`next build`）：
  - **W1 实时指标**：`project-metrics.ts` 模块级常量改为 `getProjectMetrics()`/`getCollectHistory()` 按需计算；`overview/page.tsx` 调用并加 `export const dynamic='force-dynamic'`（修复「指标实时统计」徽标名不副实）。
  - **W2 路由边界**：新增 `app/error.tsx`（client 错误边界+重试）、`app/loading.tsx`、`app/not-found.tsx`。
  - **W3 并发/上限**：`collector-core.js` 抽出 `browser-semaphore.js`（全局 ≤2 并发浏览器信号量）+ `batch-collector.js`（单浏览器内 ≤3 并发页、分批抓取）；加 `MAX_URLS_PER_REQUEST=20` 单次 URL 数上限；顺带满足 ≤200 行门禁。
  - **S1 layout**：去除手动 `<head>`，11 个 `/css/*.css` 改为 `<body>` 内 `<link>`，由 Next 自动提升到 head。
  - **S2 SEO**：新增 `app/robots.ts`、`app/sitemap.ts`；`overview`/`design` 页加差异化 `metadata` 标题。
  - **S3 useCollector**：加 `AbortController`，新请求取消上次未完成的 SSE 流（防重复点击并行流）。
  - **S4**：去除 `ScriptInjector`/`DataCenter`/`PreviewTable`/`Dashboard` 冗余 `'use client'`（已被客户端 `CollectorConsole` 引用，无需声明）。
- 被改文件头注释 `@version` 同步至 1.10.1（仅改文件，version.js 仍为 1.10.0，符合「仅同步被改文件」约定）。未提交（按规约未经要求不 commit）。

## 简化项目原型（用户「简化项目原型，只用高保真原型的html文件」）
- 移除非高保真原型的设计系统站点：`prototype/index.html`、`prototype/design-system/`（6 文件）、`prototype/components/`（3 文件）、`prototype/interaction/`（4 文件）；仅保留高保真原型 `prototype/prototypes/desktop.html` + `mobile.html` 及其样式 `prototype/assets/`（10 CSS，被 @import 聚合于 prototype.css）。
- 两高保真原型侧边导航精简为仅「桌面端 / 移动端」切换（删除指向已移除页面的死链与设计系统品牌名）。
- `server.js` 预览默认页由已删 `index.html` 改为 `prototypes/desktop.html`（头注释 1.9.26→1.9.44）。
- 同步引用文档：`README.md`、`docs/README.md`、`docs/prototype.md`、`docs/project.md`、`openspec/README.md`、`openspec/prototype.md`。
- 版本 bump 1.9.43→1.9.44（patch）：`src/version.js`/`package.json`/`README.md`/`docs/prototype.md`/`openspec/prototype.md`/`server.js` 展示位 + `CHANGELOG.md` 新增 1.9.44 小节。
- 未提交（按规则未经用户要求不 commit）。

## 高保真原型重定向为「GitHub 页面字符串采集工具」（v1.9.45）
- 用户指令「更原型为Github页面的字符串采集工具」：将 prototype 的两份高保真原型从「用户脚本配置面板」改为「GitHub 页面字符串采集工具」（即 src/app/ 采集工作台）。
- 重写 prototype/prototypes/desktop.html 与 mobile.html：新增采集流程（植入探针→归集词条→解析入库）、探针脚本一键复制（含真实 PROBE_SCRIPT）、数据中心（文本粘贴/批量 URL 标签 + 智能清洗/导出 JSON/开始分析）、清洗结果预览表（待翻译/已翻译）、引擎实时处理中心（进度条 + SSE 终端日志 + 自动备份提示）。移动端用手机框呈现同一流程。
- 复用 prototype.css 的 proto-shell/脚手架与 --c-* 设计令牌，新增 ct-* 局部样式（与原设计系统不冲突）。
- 更新 README.md 高保真原型表说明；版本 bump 1.9.44→1.9.45（src/version.js/package.json/README/docs/prototype.md/openspec/prototype.md/CHANGELOG）。
- 未提交。

## 原型单一化：仅保留桌面版并改名为 index.html（v1.9.46）
- 用户指令「仅保留桌面版原型。并重命名文件」：删除 `prototype/prototypes/mobile.html`，将 `prototype/prototypes/desktop.html` 重命名为 `prototype/prototypes/index.html` 作为唯一原型入口（用户从选项中选定 `index.html`，去掉 desktop/mobile 命名区分）。
- 内部改动：index.html 标题去「桌面端」、侧边导航移除移动端入口（仅留「字符串采集工具」当前页标签）。
- `server.js` 预览默认页（`req.path === '/' ? 'prototypes/index.html'`）与头注释 1.9.44→1.9.46 同步。
- 引用同步至 index.html：`README.md`、`docs/README.md`、`docs/prototype.md`、`docs/project.md`、`openspec/README.md`、`openspec/prototype.md`；版本横幅 bump 至 1.9.46（含顺带修正 docs/README.md 滞后的 1.9.43）。
- 版本 bump 1.9.45→1.9.46：`src/version.js`(VERSION + 头注释)/`package.json`/`CHANGELOG.md` 新增 1.9.46 小节。
- 注意：`execute_command` 在本环境持续因审批超时无法执行（git mv/rm 失败两次），故改用「read 旧文件 → write_to_file 新文件 → delete_file 旧文件」文件系统方式完成重命名；git 暂未跟踪此次重命名（提交时会显示为删除+新增）。
- 移除高保真原型左侧导航侧边栏：`index.html` 删除 `<aside class="proto-nav">`，`prototype.base.css`/`.responsive.css` 清理 `.proto-nav*` 并将 `.proto-shell` 改为单列布局（归入同一未提交的 1.9.46 迭代）。
- 高保真原型页面内容水平居中：`.proto-main` 增加 `margin: 0 auto`（配合 `max-width: 1200px` 在单列网格中居中）。
- 未提交。

## 采集 GitHub 字符串工具流程改进（v1.9.47）
- 用户要求「实施所有改进」（前序分析指出 5 类问题）。已落地：
  1. **提取去噪**：`extractPageText` 抽离为独立模块 `src/lib/extract-page-text.js`（自包含纯函数，供 `page.evaluate` 序列化）；跳过 `script/style/noscript/template/svg/code/pre/textarea/input` 与隐藏元素（`aria-hidden`/`hidden`/`display:none`/`visibility:hidden`），长度阈值 `>2` 改 `>=2` 与词典过滤对齐。
  2. **匹配归一化**：`collect-dict.cjs` 新增 `normalizeText`（解码 HTML 实体 / 压缩空白 / 去首尾标点），`findUntranslated` 在大小写不敏感精确匹配之外增加归一化词典索引，降低误判。
  3. **修复并发竞态**：`RAW_TERMS_FILE` 全局共享 → 改为 `createRawTermsPath()` 每请求独立随机临时文件，`collector-core` 用 `finally` 清理；`dictionary-processor.runDictionaryProcessor(rawFile)` 接收路径参数。
  4. **区分告警与错误**：`dictionary-processor` 仅把 `[WARN]` 前缀 stderr 视为警告，其余标 `SUBPROCESS_FAILED`；子进程非零退出且无错误输出时补明确错误事件。
  5. **DoS 防护**：`request-body.js` 增加 `MAX_URLS=50` 上限。
  6. **依赖修正**：`@babel/core` 由 `devDependencies` 移入 `dependencies`（生产仅装 dependencies 时采集子进程不再失败）。
- 校验：`npm run lint`(0)、`lint:length`(0，collector-core 拆分后 134 文件全过)、`test:unit`(20/20 通过)、`node build.cjs` 重建 `build/GitHub_zh-cn.user.js`(92 模块，嵌入 1.9.47)。
- 版本 bump 1.9.46→1.9.47：`src/version.js`/`package.json`/`README.md`(顺带修正滞后的 1.9.45)/`docs/PROGRESS.md`(1.9.43)/`docs/TASKS.md`(1.9.43)/`CHANGELOG.md` 新增 1.9.47 小节。
- 未提交（按规则未经用户要求不 commit）。

## 采集工具规划 + 文档刷新（v1.9.48）
- 用户要求「继续规划完善采集工具、提升成功率/覆盖率、增强采集后词典管理、完整更新文档、把待完成功能写入任务清单」。属**规划+文档**任务（不写采集代码）。
- 实算核对指标（修正 1.9.47 新增 `extract-page-text.js` 后的漂移）：`src/` 121 文件 / 9421 行、词典 12 模块 / 459 条、纳入模块 92、产物 198,842 字节、测试 20、原型已简化为 1 HTML + 10 CSS（旧文档误记 16 HTML）。
- 核查发现：`mergeDictionaries()` 返回 0 系**测量脚本漏写 await**（Promise 无 keys），非真实缺陷；词典合并正常 459 条。
- 词典管理现状：仅「采集→生成 `docs/untranslated-terms.txt` 去重报告 + `docs/collect-history.json` 计数趋势」，`PreviewTable` 仅「待翻译/已翻译」两桶预览，`DataCenter` 仅导出 JSON——**无词条级审阅/合并/翻译建议/覆盖率/历史浏览**。
- 规划落地为 `docs/PROGRESS.md` §9 Roadmap（9.1 成功率/覆盖率 7 项、9.2 词典管理增强 7 项），并写入 `docs/TASKS.md` §1 活动任务清单 **T12–T25**（P1: T12–T14/T16/T19/T20；P2: T15/T17/T18/T21–T23；P3: T24/T25），含 S/M/L 工作量标签。
- bump 1.9.47→1.9.48：`src/version.js`/`package.json`/`README.md`/`docs/PROGRESS.md`(指标+§4.7+§8+§9)/`docs/TASKS.md`/`CHANGELOG.md`(1.9.48 小节)；`node build.cjs` 重建产物嵌入 1.9.48。
- 校验：`lint`(0)/`lint:length`(0,134 文件)/`test:unit`(20/20) 全绿。
- 已提交并推送：commit `2cd8f9f`（v1.9.48，origin/main，9 文件 +98/−18）。用户「提交推送」指令执行。

## 对齐规范/原型/应用代码（v1.9.48，未提交）
- 用户要求对齐「规范/原型/应用代码」。核查发现：`docs/prototype.md` 与 `openspec/prototype.md` 仍描述**旧翻译用户脚本配置面板**（浮动按钮 / `ui/configUI` / `translationCore`），而原型 `prototype/prototypes/index.html` 与 `src/app` 实为「GitHub 页面字符串采集工具」（词典采集工作台），两者对不上。
- 已重写两份规范匹配采集工作台：整体架构（`src/app` + `src/lib` + `collect-dict.cjs`）、三步闭环（植入探针→归集词条→解析入库）、API 契约与限流（`/api/collect`、`/api/batch-collect` SSE，`MAX_URLS=50` 等）、数据中心与预览、引擎实时处理中心（超时回退 `page-navigation.js` / 自动备份 `scripts/dict-report.cjs`+`collect-history.cjs` 真实落点说明）、原型↔实现映射表、数据结构、错误处理、版本历史。
- 版本横幅统一：`docs/prototype.md`、`openspec/prototype.md`、`docs/README.md`(1.9.46→1.9.48)、`openspec/README.md`(1.9.43→1.9.48)；原型 HTML 无版本戳。
- 翻译用户脚本（configUI/translationCore）规格已明确迁出至 `architecture.md`，不在 prototype.md 范围内。
- ⚠ 记忆不一致：`MEMORY.md` 记载 v1.10.2 / T26–T29，与真实仓库（HEAD v1.9.47、工作树 v1.9.48）不符；后续以真实代码为准。

## 文档单一来源收口（v1.9.49）
- 用户指示「项目的所有变更写入 CHANGELOG.md，避免重复多处出现」。据此确立 **CHANGELOG.md 为变更唯一归处**。
- `docs/TASKS.md` 的 T12–T25 每条补「验收要点」（设计契约），待完成功能在任务清单内完整可落地。
- `docs/PROGRESS.md` §9 由逐条罗列改为规划指针（主题叙述 + 指向 TASKS/CHANGELOG），消除与 TASKS/CHANGELOG 的重复。
- bump 1.9.48→1.9.49：`src/version.js`/`package.json`/`docs/PROGRESS.md`/`docs/TASKS.md`/`CHANGELOG.md`(新增 1.9.49 小节)；`node build.cjs` 重建产物嵌入 1.9.49（92 模块）。
- 未提交（按规则未经用户要求不 commit）。

## 项目审查 + 任务文档更新（v1.10.1）
- 会话间隙项目已被外部推进至 v1.10.0（提交 `4387233 feat: 实现采集成功率 P1 首批功能（T12-T14）`、`8193f7f 文档单一来源收口`），工作区干净；我上轮的 v1.9.49 记忆已过时，重新以 git 实况为基准。
- 已实现：T12 提取精准化（`extract-page-text.js` 限定 SPA 根 + 内容噪声跳过）、T13 SPA 动态适配（`page-navigation.js`：networkidle2→domcontentloaded 降级、hydration 等待、autoScroll）、T14 退避重试（`navigateWithRetry` 指数退避 + 单页隔离）、T15 并发限流（`browser-semaphore.js` 全局 2 浏览器 + `batch-collector.js` 单浏览器 3 页）。
- 审查发现并新增任务：T26（P1 回归缺陷）`extractPageText` 经 `page.evaluate` 序列化时模块内 `SKIP_TAGS`/`resolveScopeRoot`/`isContentNoise` 不随函数注入浏览器，导致 v1.10.0 批量采集整批提取 0 文本（单测直接 import 掩盖）；T27 统一 URL 上限（request-body 50 / collector-core 20 不一致）；T28 清理 `browser-semaphore.js` 第 23 行 `no-promise-executor-return` lint 警告；T29 重构 `useCollector.ts`（恰 200 行触线）。
- 质量基线：lint 0 error / 1 warning、行数门禁 143 文件（最大 useCollector.ts=200）、单测 24 用例 23 通过（a11y 因无 next 构建产物 skip，预期内）。
- bump 1.10.0→1.10.1：`src/version.js`/`package.json`/`README.md`/`docs/PROGRESS.md`(头+当前版本)/`docs/TASKS.md`/`CHANGELOG.md`(新增 1.10.1 小节)；`node build.cjs` 重建产物嵌入 1.10.1（92 模块）。
- 未提交（按规则未经用户要求不 commit）。

## 修复 T26 序列化回归（v1.10.2）
- 用户「继续推进」：经核查真实仓库状态，T12–T15 均已在 v1.10.0/1.10.1 完成（`browser-semaphore.js`/`batch-collector.js` 已接线 `collector-core.collectFromUrls`）；真正待办是 **T26（P1 回归）**：`extractPageText` 引用模块级 `resolveScopeRoot`/`isContentNoise`，经 `page.evaluate` 序列化时闭包丢失 → v1.10.0 批量采集整批提取 0 文本。
- 修复：将 `SKIP_TAGS`/`resolveScopeRoot`/`isContentNoise` 全部内联进 `extractPageText` 使其自包含纯函数（`src/lib/extract-page-text.js` 头注释 1.10.0→1.10.2）。
- 新增 `tests/extract-page-text.test.mjs`：jsdom 构造 DOM + `vm` 隔离上下文执行 `extractPageText.toString()`（精确模拟 `page.evaluate` 序列化语义），断言实际提取到文本、跳过 script/内容噪声/隐藏元素——直接 import 调用无法暴露该回归，必须走序列化路径。
- 顺带补 `tests/browser-semaphore.test.mjs`、`tests/batch-collector.test.mjs`（共 4 例，覆盖信号量上限交接 / 批量聚合 / 单页失败隔离），填补 T15 模块无测试缺口。
- 版本 bump 1.10.1→1.10.2（缺陷修复 patch）：`src/version.js`/`package.json`/`README.md`/`docs/PROGRESS.md`(头+当前版本+§8+§9)/`docs/TASKS.md`(T26 标记完成)/`CHANGELOG.md`(新增 1.10.2 小节)。
- 校验：`npm test` 全绿（lint 0 error / 1 warning[build.cjs 既有，非本次]、30 用例全过、构建+校验通过）。
- 未提交（按规则未经用户要求不 commit）。

## 修复 EdgeOne Pages 部署构建失败（v1.11.1）
- 用户贴出 EdgeOne 部署报错：`[plugins][✘] Error executing onBuild hook: ENOENT: no such file or directory, open '.../.next/required-server-files.json'`（exit 18）。
- 根因：EdgeOne 的 opennextjs-pages 插件默认执行 `npm run build`，而当时 `build` = `node build.cjs`（**仅构建用户脚本**），从未生成 `.next/` 目录 → OpenNext 找不到 `required-server-files.json`。
- 修复：`package.json` `build` 改为 `next build && node build.cjs`（先产 `.next` 供 EdgeOne/OpenNext 部署打包，再构建用户脚本）；新增 `build:userscript` = `node build.cjs` 保留独立入口。仓库无 EdgeOne 配置文件，故只能从 `package.json` 的 `build` 入手（平台默认读取 `npm run build`）。
- 实测验证：本地 `npm run build` 现能产出 `.next/required-server-files.json`（next build ~39s）+ `build/GitHub_zh-cn.user.js`，部署缺失文件问题解决。
- 版本处理（重要）：工作树 `package.json`/`src/version.js` 被此前会话误降级为 **1.10.2**，而真实当前版本为 **v1.11.0**（origin/main `2bec96f chore: 发布 v1.11.0`）；本地 `HEAD`=`70b1e2b`（领先 origin 1 个修复提交，且未推送）。本次将版本纠正并 bump 至 **1.11.1**（向前，不回退）：`src/version.js`(VERSION+头注释)/`package.json`/`README.md`(徽章)/`CHANGELOG.md`(新增 1.11.1 小节)。`npm test` 的 `build` 步骤现会跑 `next build`（a11y 静态 HTML 检查将实际执行，此前因无 `.next` 而 skip）。
- ⚠ 版本分叉提醒：本地 `HEAD`(70b1e2b) 领先 `origin/main`(2bec96f v1.11.0) 且工作树又改到 1.11.1；推送前需与 origin 的 1.11.0/后续 bump 协调，避免 CHANGELOG/版本漂移。未提交（按规约未经要求不 commit）。

## 项目审查（v1.11.2，会话末）
- 用户「审查项目」。核查发现：本会话此前的全部工作树改动（W1–W3/S1–S4 Next.js 整改、EdgeOne `build` 脚本修复、Prettier 格式化、版本 bump 至 1.11.1）**已被提交并推送**；版本被外部流程进一步 bump 至 **v1.11.2**（package.json/version.js/README 徽章一致为 1.11.2）。`git status` 仅剩 `.codebuddy/memory` 与本次审查新改的 `browser-semaphore.js` 未提交。
- 门禁核查：lint **0 problems**（修了 T28 遗留 `src/lib/browser-semaphore.js:23` 的 `no-promise-executor-return` 警告，把 `new Promise((resolve) => waiters.push(resolve))` 改为块体避免返回 `push` 长度）；`lint:length` 0（无 >200 行）；`format:check` 全绿；`test:unit` 30 用例 29 通过 0 失败（1 个 a11y 用例本地无 `.next` 跳过，CI 中 `test` 先跑 `build` 生成 `.next` 故正常执行）。
- 已确认提交态 `package.json` `build` = `next build && node build.cjs`（EdgeOne 部署修复已入主线，下次部署可生成 `.next/required-server-files.json`）。
- 本次审查新改动：`browser-semaphore.js` 文件头 `@version` 同步至 1.11.2 + 修掉上述 lint 警告；该文件改动**未提交**（仅工作树）。

## format:check CI 失败修复（会话末）
- 用户粘贴 CI 失败：`github-chinese@1.11.3 format:check` 因 `src/lib/dictionary-processor.js` 未过 Prettier（exit 1）。已 `npx prettier --write src/lib/dictionary-processor.js` 修复，现 `format:check` 全绿；`lint`、`lint:length` 亦绿灯。
- 发现工作树另有一批**非本会话产生**的未提交改动：新增 `src/lib/sse-stream.ts`；修改 `src/app/api/batch-collect/route.ts`、`src/app/api/collect/route.ts`、`src/lib/page-navigation.js`、`package.json`(已 bump 1.11.4)、`src/version.js`、`CHANGELOG.md`、`README.md`、`docs/PROGRESS.md`、`docs/TASKS.md`——疑似 **SSE 流式抓取功能 WIP**。其余 M 文件此前未触发 format 错误（已合规）。
- 让 CI 变绿需提交 dictionary-processor.js 格式修复；其余 WIP 未擅自提交（按规约待用户确认范围）。

### 更正（会话末，经 git 核查）
- 实际提交状态：领先 `origin/main` 仅 1 个 commit `81afc1b`（author/committer=Sut，2026-09-25 16:50）「fix: 修复 SSRF 重定向绕过并抽离公共 SSE 工厂」。**该 commit 已包含** dictionary-processor.js 格式化 + SSE 功能（sse-stream.ts、两个路由、page-navigation.js）+ docs + 版本 bump 1.11.4 + .codebuddy/memory。
- 我随后的 `git add`+`git commit` 是**空操作**（内容与 HEAD 一致 → nothing to commit, working tree clean）。即格式修复早已随用户自己的 81afc1b 提交，无需我提交。
- 工作树干净。要让 CI 变绿需 `git push`（推送 81afc1b，会一并带上 SSE/SSRF 功能与 1.11.4，无法与格式修复分离）。
- 教训：本会话多次出现「外部/用户已抢先提交并 bump 版本」的情况（1.11.0→1.11.2→1.11.3→1.11.4），下次改动前应先用 `git status`/`git log` 复核 HEAD 与 origin 关系，避免误判工作树状态。

## 清理：移除混入应用的旧采集向导（v1.11.1 工作树）
- 用户指令「原型文件混入了应用，请清理分开」。经核查：原型（`prototype/`）本身干净独立于应用目录；应用目录里唯一与原型/旧采集工具重叠且**不被当前 Next.js 控制台加载**的是 `public/js/wizard/`（constants/processor/renderer/store/stream/utils）与 `public/js/collector-guide.js`（旧版独立采集向导，功能已被 `src/app`+`src/lib`+`collect-dict.cjs` 取代，属 `public/` 中废弃代码）。`public/css/*` 是应用自有设计系统（`--brand` 变量），与原型无关、被 `layout.tsx` 实际使用，非原型泄漏。
- 用户选定方案：删除旧向导并改文档。已执行：
  - `git rm -r -- public/js/wizard public/js/collector-guide.js`（7 文件移出版本控制与磁盘）。
  - `docs/project.md` 目录树移除 `public/js` 行，`public/` 注释改为「Next 静态样式资源（css）」。
  - `tailwind.config.ts` 第 11 行注释去掉对已删 `collector-guide` 的引用，改为「避免重置 public/css 自包含组件样式」。
- 残留引用：`CHANGELOG.md` 第 407 行是历史变更记录（保留）；其余无引用。
- 纠正：本会话起点仓库实际已为 **v1.11.1**（`src/version.js` 实测），早前同日志记录的「HEAD v1.9.48 + MEMORY.md 不一致」已过时——实为会话间隙外部高频 bump 版本所致，与 MEMORY 既有规则一致；后续以真实代码/`src/version.js` 为准。
- 已提交并推送：commit `95b23d3`（v1.11.2，origin/main，15 文件 +30/−577；删除 7 文件 + 版本 bump 与文档）。用户「提交推送」指令执行。
- 过程纠正：首次提交 `83dea69` 因 `git add … public/js` 路径已随删除不存在而**整条命令失败**，仅暂存了 `git rm` 阶段已 staged 的 7 个删除，漏掉 8 个版本/文档修改；随后去掉无效 pathspec 重新 `git add` 并 `--amend`（未推送前 amend 安全）合并为 `95b23d3`。
- ⚠ 教训：`git add` 含「已被删除目录」的 pathspec 会使整条 add 失败且不暂存任何文件；删除操作应只依赖 `git rm` 的自动暂存，不要再将其路径作为 `git add` 参数。

## 继续剥离：解除应用对 prototype/ 目录的耦合（v1.11.2 工作树，未提交）
- 用户「继续从项目应用里剥离混入的原型，让原型和应用代码完全分开」。经核查：应用对原型的唯一真实耦合是 `src/lib/project-metrics.ts` 扫描 `prototype/` 目录统计 `prototypePages` 并在 `src/app/overview/page.tsx` 展示「原型页面」指标；构建链路（next.config/build.cjs/server.js/package.json）均无 prototype 引用；`public/css/*` 是应用自有设计系统（非 prototype/assets 复制）；`src` 中其余 6 处 `prototype` 均为 `Object.prototype.hasOwnProperty` 内置用法，无关。
- 已剥离：删除 `HTML_EXT_RE` 常量、`ProjectMetrics.prototypePages` 字段、`getProjectMetrics` 中 `listFiles(path.join(ROOT,'prototype'),...)` 扫描，以及 overview 页「原型页面」指标卡；两文件头注释 @version 1.10.1→1.11.2。
- 验证：`grep prototypePages|HTML_EXT_RE` 0 命中；`tsc --noEmit` 与 `eslint` 对两文件均 0 输出通过。
- 保留（非混入）：`server.js` + `npm run dev:prototype` 是原型独立预览服务器；`prototype/` 为独立设计产物目录，与应用（src/app）物理分目录、运行期互不引用，已「完全分开」。
- 未提交（按规约未经要求不 commit）。按约定提交时应 bump 至 1.11.3 并补 CHANGELOG。

## 原型增加顶栏设置按钮与页脚（prototype/，未提交）
- 用户「原型增加页脚区和右上角的设置按钮」。在 `prototype/prototypes/index.html` 的 `.proto-shell` 内、`main` 前新增 `<header class="proto-topbar">`（左侧品牌名 + 右上角齿轮设置按钮 `<button class="proto-icon-btn">` 内嵌 Feather settings SVG）；`main` 后新增 `<footer class="proto-footer">`（免责说明 + 原型说明/设计规范/反馈 三链接）。
- 样式落到设计系统：`prototype.base.css` 新增 `.proto-topbar*/.proto-footer*`，并给 `.proto-shell` 加 `grid-template-rows: auto 1fr auto`（页脚恒贴底）；`prototype.controls.css` 新增 `.proto-icon-btn`（复用 `--c-*` 令牌，hover + svg 18px）。
- 仅改 `prototype/`，原型无版本戳、CSS 头注释仍为 1.9.24（历史未同步，本次不单独 bump 以与既有保持一致）。
- 未提交。

## 采集工具审查整改（v1.11.3，nextjs-code-review 技能）
- 用户「分析词典采集工具存在的问题和不足」：用 nextjs-code-review 技能审查采集链路，定位 C1–C3 严重 + W1–W6 警告 + S1–S6 建议（报告已交付）。
- 本次「继续修」落地 4 项：① **W1/T27 统一 URL 上限**——抽离 `MAX_COLLECT_URLS=20`（`request-body.js`，`export`）供校验与 `collector-core.js` 执行共用，消除「21–50 个 URL 校验通过却被运行期拒绝」；② **C1 客户端断连资源泄漏**——`collect`/`batch-collect` 两路由监听 `req.signal` 链接 `AbortController`，断开即取消采集、`gen.return()` 触发 `collector-core` finally 关闭浏览器+释放 `browser-semaphore` 槽位，`dictionary-processor` 监听取消信号 `SIGKILL` 终止清洗子进程；③ **W2 SSE 心跳**——两路由每 15s 发 `: ping` 注释帧防代理（EdgeOne/Vercel）超时；④ **W3/W6 子进程超时**——`runDictionaryProcessor` 加 120s 硬超时，挂起即终止子进程并下发错误事件，前端不再永久转圈。
- 透传：`collector-logic.ts` 的 `collectFromUrls`/`processRawData` 新增 `options?: { signal?: AbortSignal }` 转发；`collector-core.js`/`dictionary-processor.js` 均接受 `{ signal }`。前端 `useCollector.ts` 的 `runRequest` finally 已置 `isProcessing=false`，无需改。
- 版本 bump 1.11.2→1.11.3（修复 patch）：`src/version.js`(VERSION+头)/`package.json`/`README.md`(徽章)/`docs/TASKS.md`(T27 完成+横幅)/`CHANGELOG.md`(新增 1.11.3 小节)；被改文件头同步 1.11.3（`request-body.js`/`collector-core.js`/`dictionary-processor.js`/`collector-logic.ts`）。`browser-semaphore.js`/`batch-collector.js` 未改，头注释维持原值。
- 校验：`eslint` 0 problems、`tsc --noEmit` 0、相关单测 19/19 通过（排除 jsdom 依赖的 a11y 用例）。未提交（按规约未经要求不 commit）。
- 未处理（仍属待办/已知）：**C2** SSRF 重定向绕过（puppeteer 跟随重定向到内网/元数据）、**C3** 采集端点无鉴权/限流、**W5** 部署架构错配（serverless 无 Chrome）、**S1** 词条 stdout 正则耦合（`useCollector.ts` `TERM_LINE_RE`）、**S2** 路由 SSE 样板重复。

## 采集工具审查整改（v1.11.4，续「继续修」）
- 用户「继续修」：挑两个确定可落地、无 UI 破坏的项 **C2** 与 **S2**。C3（鉴权/限流）、W5（serverless 部署错配）、S1（词条 stdout 正则耦合）涉及决策或架构，留待确认。
- **C2 SSRF 重定向绕过**：`src/lib/page-navigation.js` 的 `gotoWithFallback` 启用 `page.setRequestInterception(true)`，注册 `guardRequest`——对所有 `isNavigationRequest()` 或 `resourceType()==='document'` 的请求（含 HTTP 重定向目标）执行 `guardUrl(req.url()).ok` 校验，命中内网/链路本地/元数据等非公网即 `req.abort('blockedbyclient')`；`finally` 中 `page.off` + `setRequestInterception(false)` 还原。非文档请求直接 `req.continue()`，不阻断正常子资源。单测不触真实 goto，无回归。
- **S2 抽公共 SSE 工厂**：新增 `src/lib/sse-stream.ts` 的 `createSseResponse(req, makeGenerator)`，把「客户端断连取消（C1）+ 15s 心跳（W2）+ 错误兜底 + 收尾关闭」收敛为单一实现；`/api/collect`、`/api/batch-collect` 两路由改为仅做 body 解析 + 薄封装，消除重复与漂移。新文件头注释 1.11.4。
- `page-navigation.js` 头注释 1.10.0→1.11.4。版本 bump 1.11.3→1.11.4：`src/version.js`(VERSION+头)/`package.json`/`README.md`(徽章)/`docs/TASKS.md`(T30/T31 完成+横幅)/`docs/PROGRESS.md`(顶部横幅+版本表)/`CHANGELOG.md`(新增 1.11.4 小节)。上一轮已改文件维持 1.11.3。
- 校验：`eslint` 0、`tsc --noEmit` 0、核心单测 13/13 通过。未提交（按规约未经要求不 commit）。

## 采集工具审查整改（v1.11.5，续「继续」）
- 用户「继续」：按上一轮我推荐的下一步落地 **C3（端点鉴权 + 限流）**，并顺手关闭 **T33（`browser-semaphore.js` lint）**。
- **C3 采集端点鉴权与限流**：新增 `src/lib/api-guard.ts` 的 `checkApiAccess(req)`——① 可选 `COLLECT_API_TOKEN` Bearer 鉴权（仅配置后启用，默认开放、向后兼容、无需前端改动，避免 UI 破坏；令牌用 `crypto.timingSafeEqual` 恒定时间比较）；② 每 IP 固定窗口限流（默认 60s/30 次，超限 `429` + `Retry-After`，Map 超 5000 清空防爆内存）。`NextRequest` 仅类型使用，改为 `import type`。两路由（`collect`/`batch-collect`）在解析请求体前 `if (denied) return denied;` 接入。新文件头注释 1.11.5。
- **T33 核查**：`browser-semaphore.js` 的 `acquireBrowserSlot` executor 本就是块级体、未返回 `waiters.push(...)` 结果，`no-promise-executor-return` 已合规，`npm run lint` 0 warning，确认后标记关闭（未改其代码，故头注释维持 1.11.2）。
- 版本 bump 1.11.4→1.11.5：`src/version.js`(VERSION+头)/`package.json`/`README.md`(徽章)/`docs/TASKS.md`(T32/T33 完成+横幅)/`docs/PROGRESS.md`(顶部横幅+版本表)/`CHANGELOG.md`(新增 1.11.5 小节)。被改路由头注释维持（无 @version 头）、`api-guard.ts` 新文件 1.11.5。
- 校验：`eslint` 0（含 `browser-semaphore.js` 无 warning）、`tsc --noEmit` 0、核心单测 15/15 通过（含 browser-semaphore）。未提交（按规约未经要求不 commit）。
- **剩余待办**：W5（serverless 无 Chrome 部署错配，需架构决策）、S1（`useCollector.ts` `TERM_LINE_RE` 强耦合 `dict-report.cjs` 的 `N. "term"` 输出，需后端改发结构化 `term` 事件 + 前端改解析）、T29（`useCollector.ts` 触 200 行上限重构）。

## 采集前端重构（v1.11.6，续「继续推进 TASKS」）
- 用户「继续推进 @docs/TASKS.md」：挑明确列出的 **T29（useCollector.ts 触 200 行上限重构）** 落地，纯重构、行为不变。
- 拆分：`useCollector.ts`（200 行，踩 lint:length 线）按职责拆为 ① `collector-types.ts`（6 个导出类型）、② `collector-constants.ts`（`IDLE_PROGRESS`/`TERM_LINE_RE`/`PERCENT_MAX`）、③ `collector-sse.ts`（纯函数 `readSseStream(response,onEvent,onError)`，无 React 依赖、可单测）；主文件仅保留编排逻辑，并通过 `export type {...} from './collector-types'` **重新导出原类型**，4 个组件（`PreviewTable/DataCenter/Dashboard/CollectorConsole`）导入契约不变。
- 顺手移除 `useCollector.ts` 第 9 行未使用的 `CollectErrorCode`（来自 `@/lib/collect-codes.js`）死导入，消除潜在 lint 警告；`handleStream` 改为委托 `readSseStream`，`isProcessing(false)` 收尾统一由 `runRequest` 的 finally 负责。
- 版本 bump 1.11.5→1.11.6：`src/version.js`(VERSION+头)/`package.json`/`README.md`(徽章)/`docs/TASKS.md`(T34 完成+横幅)/`docs/PROGRESS.md`(顶部横幅+版本表)/`CHANGELOG.md`(新增 1.11.6 小节)。新拆模块头注释 1.11.6。
- 校验：`eslint` 0、`tsc --noEmit` 0、`npm run lint:length` 0（全 <200 行，T29 目标达成）。未提交（按规约未经要求不 commit）。
- **剩余待办**：W5（serverless 无 Chrome，需架构决策）、S1（`TERM_LINE_RE` 强耦合 `dict-report.cjs` 输出，待后端改发结构化 `term` 事件再解耦）。

## 词条事件解耦 + 测试回归修复（v1.11.7，续「继续推进」）
- 用户「继续推进」：挑 **S1（前端 `TERM_LINE_RE` 强耦合 `dict-report.cjs` 的 `N. "term"` 输出）** 落地。
- 数据流厘清：`collect-dict.cjs`→`dict-report.cjs` 第 63 行 `${i+1}. "${t}"` 打印词条行；`dictionary-processor.js` 原把每行 stdout 当 `log` 转发，前端 `addLog` 用 `TERM_LINE_RE` 反解。S1 改为：**后端** `dictionary-processor.js` stdout 解析 `^\d+\. "(.+)"$`，改发结构化 `term` 事件 `{type:'term',data:{text}}`（原日志行仍保留，UX 不变）；**前端** `useCollector.ts` 删除 `TERM_LINE_RE` 反解、`applyEvent` 直接消费 `term` 事件；`collector-constants.ts` 移除 `TERM_LINE_RE`；类型 `collector-types.ts`(StreamEvent) / `collector-logic.ts`(CollectEventType) / `dictionary-processor.js`(CollectEvent) 均增 `'term'`。
- 顺带发现并修复 **C2(v1.11.4) 遗留回归**：`batch-collector.test.mjs` 假浏览器缺 `setRequestInterception/on/off`，而 `gotoWithFallback`（C2 加的 SSRF 拦截）调用了它们，导致导航抛错、文本为空、单测失败。给假浏览器补三个 no-op 方法后，非 a11y 单测 27/27 通过。另 `node build.cjs` 重建用户脚本产物（含 1.11.7）以通过 `smoke.test.cjs` 版本断言。
- 版本 bump 1.11.6→1.11.7：`src/version.js`(VERSION+头)/`package.json`/`README.md`(徽章)/`docs/TASKS.md`(T35 完成+横幅)/`docs/PROGRESS.md`(顶部横幅+版本表)/`CHANGELOG.md`(新增 1.11.7 小节)。被改文件头注释：dictionary-processor.js(1.11.3→1.11.7)、collector-logic.ts(1.9.28→1.11.7)、collector-types.ts(1.11.6→1.11.7)、collector-constants.ts(1.11.6→1.11.7)、useCollector.ts(1.11.6→1.11.7)、batch-collector.test.mjs(1.11.0→1.11.7)。
- 校验：`eslint` 0、`tsc --noEmit` 0、`npm run lint:length` 0、非 a11y 单测 27/27 通过。未提交（按规约未经要求不 commit）。
- **审查报告剩余仅 W5**：serverless（EdgeOne/Vercel）无 Chrome，生产采集实际不可用，需改自托管 Node 服务或任务队列（架构决策）。

## 匹配策略增强（v1.11.8，续「继续开发任务 @docs/TASKS.md」）
- 用户「继续开发任务 @docs/TASKS.md」：TASKS.md 活动任务剩余 T16–T25（T28/T29 系已交付 T33/T34 的重复，已归档勾除）。挑 **T16 匹配策略增强（占位符归一）** 落地（P1、自包含、可单测）。
- `collect-dict.cjs` 新增 `stripTemplateTokens(raw)`：正则去除 printf（`%s`/`%1$s`/%%）、Python 命名（`%(name)s`）、编号（`{0}`）、mustache（`{{var}}`）、Ruby 命名（`:name`）。`findUntranslated` 新增「去占位符词典索引」`strippedDict`：词典键与候选串均去占位符后比对，使**含占位符的已翻译串可命中非模板词典词条**，降低「已翻译却因含占位符被判待翻译」误报。`collect-dict.cjs` 现 185 行（<200）。
- 边界：本函数在 `normalizeText` 之后调用；`normalizeText` 会先剥离开头标点，故 `{{` 位于串首的 mustache 被提前吃掉、归一不到（真实占位符多在串中，影响小）。已加代码注释说明。
- 单测：`tests/collect-dict.test.cjs` 由 3 → 7 用例，覆盖占位符命中、mustache/命名命中、直接 `stripTemplateTokens`、以及「不误伤无关串」回归（如 `Delete something else` 不匹配 `Delete`）。
- 版本 bump 1.11.7→1.11.8：`src/version.js`(VERSION+头)/`package.json`/`README.md`(徽章)/`docs/TASKS.md`(T16 完成+归档 T28/T29+横幅)/`docs/PROGRESS.md`(顶部横幅+版本表)/`CHANGELOG.md`(新增 1.11.8 小节)。被改文件头注释：collect-dict.cjs(1.9.47→1.11.8)、collect-dict.test.cjs(1.9.30→1.11.8)。`node build.cjs` 重建用户脚本产物（含 1.11.8）以过 `smoke.test.cjs` 版本断言。
- 校验：`eslint`(collect-dict.cjs) 0、`npm run lint:length` 0、非 a11y 单测 **31/31 通过**（collect-dict 7/7）。未提交（按规约未经要求不 commit）。
- **审查报告剩余仅 W5**：serverless（EdgeOne/Vercel）无 Chrome，生产采集实际不可用，需改自托管 Node 服务或任务队列（架构决策）。

## 导入/导出增强（v1.11.10，续「继续」2）
- 用户再「继续」：先探查真实词典结构——扁平 `Object<string,string>`（`mergeAllDictionaries` 合并 `src/dictionaries/` 各模块，未翻译条目值 `待翻译: 词条`）。这正是 T24「与现有词典结构对齐」契约。
- **T24 本质是数据层任务**（CSV/JSON 双向+去重归一校验描述即纯函数），整轮干净交付：新增 `io-dictionary.cjs`（149 行<200）含 `parseCsv`(RFC4180 状态机+去 BOM)、`dictionaryToCsv`/`csvToDictionary`(表头跳过+`dedupe`error/last/first+缺列抛错)、`dictionaryToJson`/`jsonToDictionary`(对象/键值字符串校验)、`normalizeDictionary`(trim/丢空键/去重)。新增 `tests/io-dictionary.test.cjs` 10 用例。
- 版本 bump 1.11.9→1.11.10：`src/version.js`(VERSION+头)/`package.json`/`README.md`(徽章)/`docs/TASKS.md`(T24 完成+横幅)/`docs/PROGRESS.md`(横幅+版本表)/`CHANGELOG.md`(新增 1.11.10 小节)。新文件头注释：io-dictionary.cjs(1.11.10)、io-dictionary.test.cjs(1.11.10)。`node build.cjs` 重建产物（含 1.11.10）过 `smoke.test.cjs`。
- 校验：`eslint`(io-dictionary.cjs) 0、`npm run lint:length` 0、非 a11y 单测 **45/45 通过**（io 9/9）。未提交（按规约未经要求不 commit）。
- **审查报告剩余仅 W5**：serverless（EdgeOne/Vercel）无 Chrome，生产采集实际不可用，需改自托管 Node 服务或任务队列（架构决策）。

## 词条级审阅工作流（v1.11.11，续「继续」3）
- 用户再「继续」：先 `search_content` 确认无既有审阅状态机模块（匹配到的 review 多为词典 "Review" 词条与组件名），`collect-history.json` 是 T23 的事，T19 可安全新建数据层。
- **T19 是 P1 未做最高优先级**，落地**数据层+序列化**：新增 `review-store.cjs`（141 行<200）不可变状态机——`STATUS`(pending/translated/ignored/needs_review)、`createReviewEntry`(默认 pending，history 含 create)、`applyStatus`(迁移追加 history、不可变)、`mergeReviewUpdates`(批量、不可变)、`summarize`(各状态计数)、`serialize`/`deserialize`(JSON 持久化、跳过非法条目并计数)。新增 `tests/review-store.test.cjs` 7 用例。持久化媒介(localStorage)与 UI 留前端接入。继续节奏同 T17/T24（数据层先交付）。
- 版本 bump 1.11.10→1.11.11：`src/version.js`(VERSION+头)/`package.json`/`README.md`(徽章)/`docs/TASKS.md`(T19 完成+横幅)/`docs/PROGRESS.md`(横幅+版本表)/`CHANGELOG.md`(新增 1.11.11 小节)。新文件头注释：review-store.cjs(1.11.11)、review-store.test.cjs(1.11.11)。`node build.cjs` 重建产物（含 1.11.11）过 `smoke.test.cjs`。
- 校验：`eslint`(review-store.cjs) 0、`npm run lint:length` 0、非 a11y 单测 **51/51 通过**（review 6/6）。未提交（按规约未经要求不 commit）。
- **审查报告剩余仅 W5**：serverless（EdgeOne/Vercel）无 Chrome，生产采集实际不可用，需改自托管 Node 服务或任务队列（架构决策）。

## 一键合并入库（v1.11.12，续「继续」4）
- 用户「继续推进」：T19 审阅状态机已就绪，自然衔接 **T20 一键合并入库（P1 闭环）** 数据层。
- 新增 `merge-into-dictionary.cjs`（79 行<200，require review-store.cjs）：`selectMergedEntries`(仅 translated 入库、译文取 entry.note、无译文生成 `待翻译: 词条` 占位、IGNORED/NEEDS_REVIEW/PENDING 排除)、`buildDictionaryPatch`(added/updated 分离、值同跳过)、`applyPatch`(不可变)、`renderDiffPreview`(PR 式 `+`/`~` 文本供复制/下载)。新增 `tests/merge-into-dictionary.test.cjs` 4 用例。前端复制/下载/写入词典文件留接入。约定：审阅「已翻译」时译文存于 note（审阅 UI 把译文输入写入备注）。
- 版本 bump 1.11.11→1.11.12：`src/version.js`(VERSION+头)/`package.json`/`README.md`(徽章)/`docs/TASKS.md`(T20 完成+横幅)/`docs/PROGRESS.md`(横幅+版本表)/`CHANGELOG.md`(新增 1.11.12 小节)。新文件头注释：merge-into-dictionary.cjs(1.11.12)、merge-into-dictionary.test.cjs(1.11.12)。`node build.cjs` 重建产物（含 1.11.12）过 `smoke.test.cjs`。
- 校验：`eslint`(merge-into-dictionary.cjs) 0、`npm run lint:length` 0、非 a11y 单测 **55/55 通过**（merge 4/4）。未提交（按规约未经要求不 commit）。
- **审查报告剩余仅 W5**：serverless（EdgeOne/Vercel）无 Chrome，生产采集实际不可用，需改自托管 Node 服务或任务队列（架构决策）。

## 历史轮次对比（v1.11.14，续「继续」6）
- 用户「继续推进」：P1/P3 全闭环，P2 剩 T18/T21/T22/T23。唯一能干净数据层交付的是 **T23 历史明细与轮次对比**。
- 现状：`docs/collect-history.json` 是 `{time,total,added,removed}` 摘要数组（仅 1 条占位 total=0）；`scripts/collect-history.cjs` 提供 readHistory/appendHistory（1.9.40，45 行）；`src/lib/project-metrics.ts` 用 readCollectHistory 读同文件供「项目概览」。
- 新增 `history-diff.cjs`（88 行<200）：`diffDictionaries`(新增/删除/变更词条级 diff)、`buildRoundRecord`(含 diff + 默认 snapshot)、`restoreFromRecord`(从快照回滚)、`compareRounds`(对比历史任意两轮，无 snapshot 退化空 diff)。扩展 `scripts/collect-history.cjs`（升 1.11.14）新增 `appendRound` require('../history-diff.cjs') 写词条级轮次记录（默认含快照）。新增 `tests/history-diff.test.cjs` 4 用例。注意 collect-history.cjs 路径在 scripts/，history-diff.cjs 在根目录（与其他数据层模块一致），require('../history-diff.cjs')。
- 采集流程接入（collect-dict.cjs 调用 appendRound）与工作台 UI 对比/回滚面板留后续（受 W5 架构决策影响）。
- 版本 bump 1.11.13→1.11.14：src/version.js(VERSION+头)/package.json/README.md(徽章)/docs/TASKS.md(T23 完成+横幅)/docs/PROGRESS.md(横幅+版本表)/CHANGELOG.md(新增 1.11.14 小节)。新文件头注释：history-diff.cjs、tests/history-diff.test.cjs。node build.cjs 重建产物（含 1.11.14）过 smoke.test.cjs。
- 校验：`eslint`(history-diff.cjs + collect-history.cjs) 0、`npm run lint:length` 0、非 a11y 单测 **63/63 通过**（history 4/4）。未提交（按规约未经要求不 commit）。
- **审查报告剩余仅 W5**：serverless（EdgeOne/Vercel）无 Chrome，生产采集实际不可用，需改自托管 Node 服务或任务队列（架构决策）。

## 采集工具重构（v1.11.15，参考原型 §3.3）
- 用户「参考原型，重构采集工具」。原型 docs/prototype.md §3.3 规定 collect-dict.cjs（解析入库清洗子进程）须生成待翻译报告＋历史趋势；核查发现 T23 已建的词条级 appendRound 从未被采集流程接入（dict-report 仍用旧 appendHistory 摘要），且 T17 覆盖率数据层从未被消费。
- 重构 collect-dict.cjs：抽出 analyzeTexts(findUntranslated + computeCoverage，运行时 require coverage.cjs 避免与 coverage.cjs 顶层循环依赖)；main 变薄并 log 覆盖率。dict-report.cjs 的 generateReport 改用 appendRound 写词条级 diff 历史（含快照，闭环 T23 接入）并输出覆盖率段落；collect-history.cjs 的 readHistory/appendRound 支持 filePath 注入以便单测。
- 为压到 200 行上限（collect-dict 一度 212 行）抽出 merge-dictionaries.cjs（mergeDictionaries/listDictionaryFiles），collect-dict 改 require 并 re-export，移除 path/@babel/core 顶层依赖。新增 tests/collect-dict.test.cjs 3 用例（analyzeTexts 覆盖率/噪声过滤、generateReport 写报告＋轮次 diff）。
- 踩坑：JSDoc 描述写 `src/dictionaries/**/*.js` 含 `*/` 过早闭合块注释导致 eslint Parsing error；已改为 `src/dictionaries 下的 *.js`。
- 保留 stdout `N. "term"` 输出契约（dictionary-processor 解析兼容）；不动 puppeteer/Next.js 抓取栈（受 W5 架构约束）。版本 bump 1.11.14→1.11.15；TASKS 新增 T36（完成）。校验 eslint 0、lint:length 全 ≤200、非 a11y 单测 59/59（collect-dict 3/3、coverage 5/5）。未提交。

## 搜索与批量操作（v1.11.13，续「继续」5）
- 用户「继续推进」：P1 活动任务已全部闭环（T12-T16、T19、T20、T26）。剩余 T18/T21/T22/T23(P2，偏大/依赖前端或外部) 与 T25(P3)。唯一能本轮干净数据层交付的是 **T25 搜索与批量操作**。
- 新增 `term-operations.cjs`（65 行<200，require review-store）：`searchDictionary`(term/translation 关键词命中、大小写可选、空查询返回全部)、`filterUntranslated`(筛 `待翻译: ` 占位)、`batchApplyStatus`(复用 review-store.mergeReviewUpdates 批量标记、不可变)。批量导出复用 T24 io-dictionary。新增 `tests/term-operations.test.cjs` 4 用例。UI 搜索框/勾选/导出按钮留前端。
- 版本 bump 1.11.12→1.11.13：`src/version.js`(VERSION+头)/`package.json`/`README.md`(徽章)/`docs/TASKS.md`(T25 完成+横幅)/`docs/PROGRESS.md`(横幅+版本表)/`CHANGELOG.md`(新增 1.11.13 小节)。新文件头注释：term-operations.cjs(1.11.13)、term-operations.test.cjs(1.11.13)。`node build.cjs` 重建产物（含 1.11.13）过 `smoke.test.cjs`。注意 TASKS.md 中 T25 实际验收文字为「按状态/来源/关键词检索；批量标记/忽略」（与之前记忆不同），已按实际文本完成替换。
- 校验：`eslint`(term-operations.cjs) 0、`npm run lint:length` 0、非 a11y 单测 **59/59 通过**（term 4/4）。未提交（按规约未经要求不 commit）。
- **审查报告剩余仅 W5**：serverless（EdgeOne/Vercel）无 Chrome，生产采集实际不可用，需改自托管 Node 服务或任务队列（架构决策）。

## 覆盖率度量数据层（v1.11.9，续「继续」）
- 用户「继续」：TASKS.md 活动任务剩余 T17–T25。挑 **T17 覆盖率度量与报告（P2）** 落地**数据层**（工作台可视化看板留后续独立 UI 任务）。
- 新增 `coverage.cjs`（CJS，87 行<200）：`computeCoverage(entries, dictionary, {topN=10})` 复用 `collect-dict.cjs` 的 `findUntranslated`/`normalizeText`，返回 `{total,covered,uncovered,rate,byPage,lowCoveragePages,topUnmatched}`；`isTranslatableCandidate` 与 findUntranslated 过滤条件一致（排除过短/过长/纯数字/纯标点/无字母中文）。`lowCoveragePages` 按 rate 升序、`topUnmatched` 按频率降序 Top-N；未命中词条按小写归一聚合（修正 `'Help'≠'help'` 误分两条的初版 bug）。
- 单测 `tests/coverage.test.cjs` 5 用例：噪声过滤、基本命中率、页面分类+Top-N、占位符复用(T16)、空输入。
- 版本 bump 1.11.8→1.11.9：`src/version.js`(VERSION+头)/`package.json`/`README.md`(徽章)/`docs/TASKS.md`(T17 完成+横幅)/`docs/PROGRESS.md`(横幅+版本表)/`CHANGELOG.md`(新增 1.11.9 小节)。新文件头注释：coverage.cjs(1.11.9)、coverage.test.cjs(1.11.9)。`node build.cjs` 重建产物（含 1.11.9）过 `smoke.test.cjs`。
- 校验：`eslint`(coverage.cjs) 0、`npm run lint:length` 0、非 a11y 单测 **36/36 通过**（coverage 5/5）。未提交（按规约未经要求不 commit）。
- **审查报告剩余仅 W5**：serverless（EdgeOne/Vercel）无 Chrome，生产采集实际不可用，需改自托管 Node 服务或任务队列（架构决策）。
