# 2026-09-25

## 清理根目录冗余文件（用户「减少项目根目录下的文件数量，并去除冗余文件」）
- 根目录 17 个散落文件中，仅 2 个为**与项目交付物无关的 AI 工具产物**，已删除：
  - `GEMINI.md`（72B，Gemini 自定义指令「对话 Suggestions 保持中文」）
  - `metadata.json`（含 `MAJOR_CAPABILITY_SERVER_SIDE_GEMINI_API`，Gemini/AI Studio 项目元数据）
- 核验：全仓 `search_content` 搜 `metadata.json|GEMINI|MAJOR_CAPABILITY` 仅命中 metadata.json 自身；无 CLAUDE.md/AGENTS.md/.cursorrules 等其他 AI 指令文件；两文件均未被任何构建/脚本引用；均被 git 跟踪（来源 `9d981fa chore: improve tooling and dictionary collection`）。删除后根目录文件 17 → 15。
- 其余 15 个根文件均为必要：`build.cjs`/`collect-dict.cjs`（构建/词典）、`eslint.config.js`+`eslint/`（规范）、`next.config.mjs`/`postcss.config.mjs`/`tailwind.config.ts`/`tsconfig.json`/`next-env.d.ts`（Next/TS 配置）、`server.js`（dev:prototype）、`package.json`/`package-lock.json`、`README.md`/`CHANGELOG.md`/`CONTRIBUTING.md`/`LICENSE`。
- 主动提示但未删（属功能/资产配置，非"冗余文件"）：`prototype/`（26 个旧 HTML/CSS 原型，被 `dev:prototype`/`server.js` 使用，已被 Next 工作台取代，可未来清理）；`openspec/`（按既有约定仅作 docs/ 索引，保留）。
- 未提交（按规则未经用户要求不 commit）。

## 采集成功率 P1（T12–T14）实现 · v1.10.0
- 用户选择先推进 P1 采集成功率（T12 提取精准化 + T13 SPA/动态适配 + T14 单页鲁棒性）。
- 新增 `src/lib/page-navigation.js`（Node 侧、无浏览器依赖、可单测）：`gotoWithFallback`（networkidle2 超时降级 domcontentloaded+固定等待）、`waitForHydration`（#react-app）、`autoScroll`、`navigateWithRetry`（指数退避 1s/2s/4s、上限 3 次，识别 429/超时/网络错误）、`isRetryable`/`computeBackoffDelay`/`RetryableError`。
- `extract-page-text.js`（T12）：作用域从 `document.body` 收窄为 `resolveScopeRoot()`（#react-app → .application-main → body），并跳过 markdown-body/highlight/blob-code/CodeMirror/评论等「内容型容器」降噪。
- `collector-core.js`（T13/T14）：逐 URL `yield* navigateWithRetry` + `waitForHydration` + `autoScroll`；删去冗余常量与魔法数（429/150 抽常量）。
- 新增 `tests/page-navigation.test.mjs`（4 例）。`collector-core.js` 因超 200 行已把 `navigateWithRetry` 拆入 `page-navigation.js`，门禁 0 超限、lint 0 warning。
- 版本升 minor → **1.10.0**（`src/version.js`/`package.json`/README/CHANGELOG/PROGRESS/TASKS 同步）；`npm test` 全绿（24 用例、0 error/0 warning）。
- 注意：会话间隙 `version.js` 已被外部 bump 到 1.9.49 并随后提交为 1.10.0；当前 HEAD 已含代码/版本/测试，仅 `collector-core.js`、PROGRESS/TASKS/CHANGELOG、构建产物尚未提交。未经用户要求未 commit。

## 用户脚本文件名更名（用户「更新构建的脚本名称为 GitHub_zh-cn.user.js」）
- 将构建产物 `build/GitHub_i18n.user.js` 更名为 `build/GitHub_zh-cn.user.js`（仅改文件名，**仓库名 `GitHub_i18n` 与全局变量 `window.GitHub_i18n` 保持不变**）。
- 同步点：`build.cjs`（OUTPUT_FILE + `@updateURL`/`@downloadURL`）、`ci-cd.yml`（产物名/路径）、`src/config.js` 与 `src/versionChecker/fetcher.js`（更新检查 URL）、`src/lib/project-metrics.ts`、`src/app/overview/page.tsx`、测试/校验脚本；重新 `npm run build` 生成新产物并删旧产物（validate 通过）。
- 文档：README 命名说明改为新名并说明更名后果（旧安装无法自动更新，需重装）；`docs/`(project/PROGRESS/development/architecture) + `openspec/`(project/development/architecture 及 README/coding-style/prototype/config.yaml) 的脚本名引用与**版本横幅统一补同步到 v1.9.43**（外部 `cd8f9f9` 发布 v1.9.43 时同步不完整，漏了 project/development/architecture 横幅与 openspec）。
- 提交 `2c4ce96`（25 文件，+58/−54，git 识别为重命名 99%）。未 bump 版本（沿用外部已发布的 1.9.43；改名属配置/命名变更）。

## 纠正：GEMINI.md / metadata.json 不得清理（用户全局偏好）
- **2026-09-25 早些时候误删**了 `GEMINI.md` 与 `metadata.json`（误判为 Gemini 工具冗余产物）。用户明确：**这两个文件是 Google AI Studio 必需的，必须保留，任何「去冗余 / 减文件」任务都不得清理它们**。
- 已用 `git checkout HEAD -- GEMINI.md metadata.json` 恢复，内容校验一致（`GEMINI.md` 中文指令；`metadata.json` 含 `MAJOR_CAPABILITY_SERVER_SIDE_GEMINI_API`）。
- 已写入 `MEMORY.md`「受保护文件（勿清理 / 勿删除）」长期条目，后续会话自动跳过这两个路径。

## Next.js 代码审查整改（用户「审查项目 / 修复所有」，nextjs-code-review 技能）
- 用 nextjs-code-review 技能对 App Router 做专项审查，定位 W1–W3 + S1–S4（S5 为备注无需改码）。
- 关键前提：项目被外部高频 bump 至 **v1.10.0**（HEAD `8193f7f`，未提交本地改动含 `collector-core.js`/`build`/`docs`）。早先读到的旧 `collector-core.js`（含 `RAW_TERMS_FILE`）已过期；当前已抽出 `extract-page-text.js`、`page-navigation.js`，且**临时文件竞态在 v1.9.47 已修复**（`createRawTermsPath`+清理），故审查中 W3「竞态」子项已无需处理。
- 已落地修复（均通过 tsc/lint/lint:length/单元测试/`next build`）：
  - **W1 实时指标**：`project-metrics.ts` 模块级常量改为 `getProjectMetrics()`/`getCollectHistory()` 按需计算；`overview/page.tsx` 调用并加 `export const dynamic='force-dynamic'`（修复「指标实时统计」徽标名不副实）。
  - **W2 路由边界**：新增 `app/error.tsx`（client 错误边界+重试）、`app/loading.tsx`、`app/not-found.tsx`。
  - **W3 并发/上限**：`collector-core.js` 抽出 `browser-semaphore.js`（全局 ≤2 并发浏览器信号量）+ `batch-collector.js`（单浏览器内 ≤3 并发页、分批抓取）；加 `MAX_URLS_PER_REQUEST=20` 单次 URL 数上限；顺带满足 ≤200 行门禁。
  - **S1 layout**：去除手动 `<head>`，11 个 `/css/*.css` 改为 `<body>` 内 `<link>`，由 Next 自动提升到 head。
  - **S2 SEO**：新增 `app/robots.ts`、`app/sitemap.ts`；`overview`/`design` 页加差异化 `metadata` 标题。
  - **S3 useCollector**：加 `AbortController`，新请求取消上次未完成的 SSE 流（防重复点击并行流）。
  - **S4**：去除 `ScriptInjector`/`DataCenter`/`PreviewTable`/`Dashboard` 冗余 `'use client'`（已被客户端 `CollectorConsole` 引用，无需声明）。
- 被改文件头注释 `@version` 同步至 1.10.1（仅改文件，version.js 仍为 1.10.0，符合「仅同步被改文件」约定）。未提交（按规约未经要求不 commit）。

## 简化项目原型（用户「简化项目原型，只用高保真原型的html文件」）
- 移除非高保真原型的设计系统站点：`prototype/index.html`、`prototype/design-system/`（6 文件）、`prototype/components/`（3 文件）、`prototype/interaction/`（4 文件）；仅保留高保真原型 `prototype/prototypes/desktop.html` + `mobile.html` 及其样式 `prototype/assets/`（10 CSS，被 @import 聚合于 prototype.css）。
- 两高保真原型侧边导航精简为仅「桌面端 / 移动端」切换（删除指向已移除页面的死链与设计系统品牌名）。
- `server.js` 预览默认页由已删 `index.html` 改为 `prototypes/desktop.html`（头注释 1.9.26→1.9.44）。
- 同步引用文档：`README.md`、`docs/README.md`、`docs/prototype.md`、`docs/project.md`、`openspec/README.md`、`openspec/prototype.md`。
- 版本 bump 1.9.43→1.9.44（patch）：`src/version.js`/`package.json`/`README.md`/`docs/prototype.md`/`openspec/prototype.md`/`server.js` 展示位 + `CHANGELOG.md` 新增 1.9.44 小节。
- 未提交（按规则未经用户要求不 commit）。

## 高保真原型重定向为「GitHub 页面字符串采集工具」（v1.9.45）
- 用户指令「更原型为Github页面的字符串采集工具」：将 prototype 的两份高保真原型从「用户脚本配置面板」改为「GitHub 页面字符串采集工具」（即 src/app/ 采集工作台）。
- 重写 prototype/prototypes/desktop.html 与 mobile.html：新增采集流程（植入探针→归集词条→解析入库）、探针脚本一键复制（含真实 PROBE_SCRIPT）、数据中心（文本粘贴/批量 URL 标签 + 智能清洗/导出 JSON/开始分析）、清洗结果预览表（待翻译/已翻译）、引擎实时处理中心（进度条 + SSE 终端日志 + 自动备份提示）。移动端用手机框呈现同一流程。
- 复用 prototype.css 的 proto-shell/脚手架与 --c-* 设计令牌，新增 ct-* 局部样式（与原设计系统不冲突）。
- 更新 README.md 高保真原型表说明；版本 bump 1.9.44→1.9.45（src/version.js/package.json/README/docs/prototype.md/openspec/prototype.md/CHANGELOG）。
- 未提交。

## 原型单一化：仅保留桌面版并改名为 index.html（v1.9.46）
- 用户指令「仅保留桌面版原型。并重命名文件」：删除 `prototype/prototypes/mobile.html`，将 `prototype/prototypes/desktop.html` 重命名为 `prototype/prototypes/index.html` 作为唯一原型入口（用户从选项中选定 `index.html`，去掉 desktop/mobile 命名区分）。
- 内部改动：index.html 标题去「桌面端」、侧边导航移除移动端入口（仅留「字符串采集工具」当前页标签）。
- `server.js` 预览默认页（`req.path === '/' ? 'prototypes/index.html'`）与头注释 1.9.44→1.9.46 同步。
- 引用同步至 index.html：`README.md`、`docs/README.md`、`docs/prototype.md`、`docs/project.md`、`openspec/README.md`、`openspec/prototype.md`；版本横幅 bump 至 1.9.46（含顺带修正 docs/README.md 滞后的 1.9.43）。
- 版本 bump 1.9.45→1.9.46：`src/version.js`(VERSION + 头注释)/`package.json`/`CHANGELOG.md` 新增 1.9.46 小节。
- 注意：`execute_command` 在本环境持续因审批超时无法执行（git mv/rm 失败两次），故改用「read 旧文件 → write_to_file 新文件 → delete_file 旧文件」文件系统方式完成重命名；git 暂未跟踪此次重命名（提交时会显示为删除+新增）。
- 移除高保真原型左侧导航侧边栏：`index.html` 删除 `<aside class="proto-nav">`，`prototype.base.css`/`.responsive.css` 清理 `.proto-nav*` 并将 `.proto-shell` 改为单列布局（归入同一未提交的 1.9.46 迭代）。
- 高保真原型页面内容水平居中：`.proto-main` 增加 `margin: 0 auto`（配合 `max-width: 1200px` 在单列网格中居中）。
- 未提交。

## 采集 GitHub 字符串工具流程改进（v1.9.47）
- 用户要求「实施所有改进」（前序分析指出 5 类问题）。已落地：
  1. **提取去噪**：`extractPageText` 抽离为独立模块 `src/lib/extract-page-text.js`（自包含纯函数，供 `page.evaluate` 序列化）；跳过 `script/style/noscript/template/svg/code/pre/textarea/input` 与隐藏元素（`aria-hidden`/`hidden`/`display:none`/`visibility:hidden`），长度阈值 `>2` 改 `>=2` 与词典过滤对齐。
  2. **匹配归一化**：`collect-dict.cjs` 新增 `normalizeText`（解码 HTML 实体 / 压缩空白 / 去首尾标点），`findUntranslated` 在大小写不敏感精确匹配之外增加归一化词典索引，降低误判。
  3. **修复并发竞态**：`RAW_TERMS_FILE` 全局共享 → 改为 `createRawTermsPath()` 每请求独立随机临时文件，`collector-core` 用 `finally` 清理；`dictionary-processor.runDictionaryProcessor(rawFile)` 接收路径参数。
  4. **区分告警与错误**：`dictionary-processor` 仅把 `[WARN]` 前缀 stderr 视为警告，其余标 `SUBPROCESS_FAILED`；子进程非零退出且无错误输出时补明确错误事件。
  5. **DoS 防护**：`request-body.js` 增加 `MAX_URLS=50` 上限。
  6. **依赖修正**：`@babel/core` 由 `devDependencies` 移入 `dependencies`（生产仅装 dependencies 时采集子进程不再失败）。
- 校验：`npm run lint`(0)、`lint:length`(0，collector-core 拆分后 134 文件全过)、`test:unit`(20/20 通过)、`node build.cjs` 重建 `build/GitHub_zh-cn.user.js`(92 模块，嵌入 1.9.47)。
- 版本 bump 1.9.46→1.9.47：`src/version.js`/`package.json`/`README.md`(顺带修正滞后的 1.9.45)/`docs/PROGRESS.md`(1.9.43)/`docs/TASKS.md`(1.9.43)/`CHANGELOG.md` 新增 1.9.47 小节。
- 未提交（按规则未经用户要求不 commit）。

## 采集工具规划 + 文档刷新（v1.9.48）
- 用户要求「继续规划完善采集工具、提升成功率/覆盖率、增强采集后词典管理、完整更新文档、把待完成功能写入任务清单」。属**规划+文档**任务（不写采集代码）。
- 实算核对指标（修正 1.9.47 新增 `extract-page-text.js` 后的漂移）：`src/` 121 文件 / 9421 行、词典 12 模块 / 459 条、纳入模块 92、产物 198,842 字节、测试 20、原型已简化为 1 HTML + 10 CSS（旧文档误记 16 HTML）。
- 核查发现：`mergeDictionaries()` 返回 0 系**测量脚本漏写 await**（Promise 无 keys），非真实缺陷；词典合并正常 459 条。
- 词典管理现状：仅「采集→生成 `docs/untranslated-terms.txt` 去重报告 + `docs/collect-history.json` 计数趋势」，`PreviewTable` 仅「待翻译/已翻译」两桶预览，`DataCenter` 仅导出 JSON——**无词条级审阅/合并/翻译建议/覆盖率/历史浏览**。
- 规划落地为 `docs/PROGRESS.md` §9 Roadmap（9.1 成功率/覆盖率 7 项、9.2 词典管理增强 7 项），并写入 `docs/TASKS.md` §1 活动任务清单 **T12–T25**（P1: T12–T14/T16/T19/T20；P2: T15/T17/T18/T21–T23；P3: T24/T25），含 S/M/L 工作量标签。
- bump 1.9.47→1.9.48：`src/version.js`/`package.json`/`README.md`/`docs/PROGRESS.md`(指标+§4.7+§8+§9)/`docs/TASKS.md`/`CHANGELOG.md`(1.9.48 小节)；`node build.cjs` 重建产物嵌入 1.9.48。
- 校验：`lint`(0)/`lint:length`(0,134 文件)/`test:unit`(20/20) 全绿。
- 已提交并推送：commit `2cd8f9f`（v1.9.48，origin/main，9 文件 +98/−18）。用户「提交推送」指令执行。

## 文档单一来源收口（v1.9.49）
- 用户指示「项目的所有变更写入 CHANGELOG.md，避免重复多处出现」。据此确立 **CHANGELOG.md 为变更唯一归处**。
- `docs/TASKS.md` 的 T12–T25 每条补「验收要点」（设计契约），待完成功能在任务清单内完整可落地。
- `docs/PROGRESS.md` §9 由逐条罗列改为规划指针（主题叙述 + 指向 TASKS/CHANGELOG），消除与 TASKS/CHANGELOG 的重复。
- bump 1.9.48→1.9.49：`src/version.js`/`package.json`/`docs/PROGRESS.md`/`docs/TASKS.md`/`CHANGELOG.md`(新增 1.9.49 小节)；`node build.cjs` 重建产物嵌入 1.9.49（92 模块）。
- 未提交（按规则未经用户要求不 commit）。

## 项目审查 + 任务文档更新（v1.10.1）
- 会话间隙项目已被外部推进至 v1.10.0（提交 `4387233 feat: 实现采集成功率 P1 首批功能（T12-T14）`、`8193f7f 文档单一来源收口`），工作区干净；我上轮的 v1.9.49 记忆已过时，重新以 git 实况为基准。
- 已实现：T12 提取精准化（`extract-page-text.js` 限定 SPA 根 + 内容噪声跳过）、T13 SPA 动态适配（`page-navigation.js`：networkidle2→domcontentloaded 降级、hydration 等待、autoScroll）、T14 退避重试（`navigateWithRetry` 指数退避 + 单页隔离）、T15 并发限流（`browser-semaphore.js` 全局 2 浏览器 + `batch-collector.js` 单浏览器 3 页）。
- 审查发现并新增任务：T26（P1 回归缺陷）`extractPageText` 经 `page.evaluate` 序列化时模块内 `SKIP_TAGS`/`resolveScopeRoot`/`isContentNoise` 不随函数注入浏览器，导致 v1.10.0 批量采集整批提取 0 文本（单测直接 import 掩盖）；T27 统一 URL 上限（request-body 50 / collector-core 20 不一致）；T28 清理 `browser-semaphore.js` 第 23 行 `no-promise-executor-return` lint 警告；T29 重构 `useCollector.ts`（恰 200 行触线）。
- 质量基线：lint 0 error / 1 warning、行数门禁 143 文件（最大 useCollector.ts=200）、单测 24 用例 23 通过（a11y 因无 next 构建产物 skip，预期内）。
- bump 1.10.0→1.10.1：`src/version.js`/`package.json`/`README.md`/`docs/PROGRESS.md`(头+当前版本)/`docs/TASKS.md`/`CHANGELOG.md`(新增 1.10.1 小节)；`node build.cjs` 重建产物嵌入 1.10.1（92 模块）。
- 未提交（按规则未经用户要求不 commit）。

## 修复 T26 序列化回归（v1.10.2）
- 用户「继续推进」：经核查真实仓库状态，T12–T15 均已在 v1.10.0/1.10.1 完成（`browser-semaphore.js`/`batch-collector.js` 已接线 `collector-core.collectFromUrls`）；真正待办是 **T26（P1 回归）**：`extractPageText` 引用模块级 `resolveScopeRoot`/`isContentNoise`，经 `page.evaluate` 序列化时闭包丢失 → v1.10.0 批量采集整批提取 0 文本。
- 修复：将 `SKIP_TAGS`/`resolveScopeRoot`/`isContentNoise` 全部内联进 `extractPageText` 使其自包含纯函数（`src/lib/extract-page-text.js` 头注释 1.10.0→1.10.2）。
- 新增 `tests/extract-page-text.test.mjs`：jsdom 构造 DOM + `vm` 隔离上下文执行 `extractPageText.toString()`（精确模拟 `page.evaluate` 序列化语义），断言实际提取到文本、跳过 script/内容噪声/隐藏元素——直接 import 调用无法暴露该回归，必须走序列化路径。
- 顺带补 `tests/browser-semaphore.test.mjs`、`tests/batch-collector.test.mjs`（共 4 例，覆盖信号量上限交接 / 批量聚合 / 单页失败隔离），填补 T15 模块无测试缺口。
- 版本 bump 1.10.1→1.10.2（缺陷修复 patch）：`src/version.js`/`package.json`/`README.md`/`docs/PROGRESS.md`(头+当前版本+§8+§9)/`docs/TASKS.md`(T26 标记完成)/`CHANGELOG.md`(新增 1.10.2 小节)。
- 校验：`npm test` 全绿（lint 0 error / 1 warning[build.cjs 既有，非本次]、30 用例全过、构建+校验通过）。
- 未提交（按规则未经用户要求不 commit）。
