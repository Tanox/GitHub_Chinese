# 2026-09-23 工作日志

## 继续推进剩余任务（v1.9.27 → v1.9.28）

本轮关闭 **P2-4（配置面板性能监控按钮为死按钮）** 与 **P2-7（采集流程缺少错误码约定）**。

### P2-4 死按钮修复
- 现象：`src/ui/components/performanceMonitor.js` 里 `refreshBtn` / `exportBtn` 只 `document.createElement` 建 DOM，
  从未 `addEventListener`，点击毫无反应（renderer.js 仅挂载区块）。
- 修复：绑定 `updatePerformanceStats`（刷新）与 `exportPerformanceStats`（导出）；导出无数据（返回 null）
  时按钮短显「暂无数据」并禁用 1.5s，抽出 `NO_DATA_FEEDBACK_MS` 常量消除 `no-magic-numbers` 警告。

### P2-7 错误码约定
- 新增 `src/lib/collect-codes.js`：**纯数据模块**（不含 `fs`/`child_process`/`puppeteer`），
  服务端与客户端共用 `CollectErrorCode`（UNKNOWN/MIDDING_DEPENDENCY/FETCH_FAILED/SUBPROCESS_FAILED/INPUT_INVALID）。
  - 关键设计：该模块必须客户端可安全导入——故不能放 `collector-core.js`（含 fs/child_process，会被带入客户端包）。
- 服务端填充：
  - `collector-core.js`：`collectFromUrls([])` / `processRawData('')` 入口返回 `INPUT_INVALID`；
    缺 puppeteer → `MISSING_DEPENDENCY`；单 URL 失败 → `FETCH_FAILED`；保存/清洗失败 → `SUBPROCESS_FAILED`。
  - `dictionary-processor.js`：子进程 stderr 的 `error` 事件带 `SUBPROCESS_FAILED`。
- 前端：`collector-logic.ts` 导出 `CollectErrorCode`；`useCollector.ts` 的 `LogEntry` 加 `code`，
  `applyEvent` 错误分支透传 `event.code`；`Dashboard.tsx` 错误行渲染 `E<code>` 徽标（`.log-code` 样式加在 `showcase.css`）。

### 踩坑（重要）
- **lint-staged 不可靠修正 JSX 双引号**：`Dashboard.tsx` / `useCollector.ts` 被自动提交后工作区仍是双引号，
  `format:check` 报真实样式违规（58 / 128 行差异，非 CRLF 假阳性）。已对这两个文件 `prettier --write` 定点修正
  （仅改自己的文件，不碰那 67 个既有 CRLF 文件）。复检后 `format:check` 仅剩 67 个既有 CRLF 文件告警。
- 验证脚本去 `\r` 比对时才发现：`.split('\n')` 会残留 `\r` 导致每行都「不同」，必须 `.map(s=>s.replace(/\r$/,'')`。
- 构建清理 ENOENT 竞态修复（上轮 `force:true`）已生效，本轮未复现。

### 验证
- `npm run test`：lint 0/0、build（92 模块、194.24 KB）、validate 通过。
- `npm run build:web`：7 路由正常，仅余 puppeteer 可选依赖告警（P0-2）。
- 运行时校验：空输入 / 空 URL 均回 `code:3001`（`INPUT_INVALID`）。
- 规模：117 文件 / 8889 行（新增 collect-codes.js）。

### 文档与版本
- 版本 1.9.28（`src/version.js` + `package.json` + README 徽章 + 被编辑文件头注释）。
- `CHANGELOG.md` 新增 1.9.28 小节；`docs/PROGRESS.md`（指标 117/8889、P2-4+P2-7 关闭、§4.5 记录 E1-E3、变更记录）；
  `docs/architecture.md`（采集流要点补错误码说明 + 版本历史 + 版本行）。

### 剩余任务
P0-2（装 puppeteer）、P1-3（Jest 未启用）、P1-4（双锁漂移）、P2-3（采集增量去重）、P2-5（E2E/冒烟）。

---

## 继续推进（v1.9.28 → v1.9.29）

本轮关闭 **P1-4（双锁文件漂移）** 与 **P2-3（词典采集增量与去重统计）**，提交 `5f772c8`。

### P1-4 双锁漂移
- 删除 `bun.lock`（`git rm`），保留 npm 单一锁 `package-lock.json`；`.gitignore` 追加 `bun.lock` 防再生。
- 校验用 `npm install --dry-run --ignore-scripts`（puppeteer 在 dependencies，直接 install 会下载 Chromium）。

### P2-3 增量去重统计
- `collect-dict.cjs` 的 `generateReport` 新增 `computeDelta`：对比历史 `docs/untranslated-terms.txt`，
  输出「新增 / 移除 / 净增（历史 N → 当前 M）」并展示新增词条样例，经 SSE 透传至「处理中心」。
- 顺带精简（删调试 `sleep(20)` 与 `[模块分析]` 日志、压缩词条列表打印），守住「单文件 ≤200 行」（209→约 198）。
- 实跑验证：`node collect-dict.cjs terms_test.txt` → `新增 5 / 移除 0 / 净增 +5（历史 0 → 当前 5）`。
  ⚠️ 测试会覆盖 `docs/untranslated-terms.txt`，跑完须 `git checkout --` 还原。

### 踩坑（重要）
- **husky/lint-staged 与 HEAD 竞态**：`git commit` 报 `fatal: cannot lock ref 'HEAD': is at 5f772c8 but expected 736bdf2`，
  但提交实际已落盘（log 可见 `5f772c8`）。lint-staged automatic backup `stash@{0}` 因中断未自动清理，
  内含 pre-commit 时未纳入提交的改动（`package-lock.json` 被 dry-run 改动等）。建议保留该 stash（不丢数据），
  需要时再 `git stash drop "stash@{0}"`；**PowerShell 中 `stash@{0}` 必须加引号**，否则 `@{0}` 被解析为哈希表。
- lint-staged stash 会移走未提交改动，提交后 `git status` 看似干净实为 stash 所致，需留意。

### 剩余任务
P0-2（装 puppeteer）、P1-3（Jest 未启用）、P2-5（E2E/冒烟）。

---

## 继续推进（v1.9.29 → v1.9.30）

本轮关闭 **P1-3（清理/启用 Jest 测试体系）**，提交 `9b542f2`。

### 方案：零依赖 Node 内置 test runner
- 项目实际用 ESM 用户脚本 + Next，jest/babel-jest/jsdom 均未安装，`jest.config.js`（含 coverageThreshold）是**死配置**。
- 决策：**移除 jest 配置，改用 Node 内置 `node --test`**（本机 Node 26.4.0，零新增依赖，契合 ESM）。
- 删除：`jest.config.js`、`jest.setup.js`。
- 新增 `tests/`：`collect-codes.test.mjs`（错误码契约：冻结 + 取值唯一 + 关键键齐全）、
  `collect-dict.test.cjs`（`findUntranslated` 命中判定 / 大小写不敏感 / 过滤规则），共 5 用例全通过。
- `package.json`：新增 `"test:unit": "node --test"`；`"test"` 改为 `lint → test:unit → build → validate`；
  `lint` / `format` 纳入 `tests/`。

### 踩坑（重要）
- **`node --test tests/`（传目录）在 Node 26 报 `Cannot find module 'tests'`**（被当作模块路径）。
  必须用**无参 `node --test`**（自动发现 `**/*.test.{js,mjs,cjs}`，默认排除 node_modules）。
- 新文件须 `prettier --check` 复核：`collect-codes.test.mjs` 长行被 prettier 换行修正后才合规。

### 剩余任务
P0-2（装 puppeteer）、P2-5（E2E/冒烟）。

---

## 继续推进（v1.9.30 → v1.9.32）—— 遗留任务全部清零

本轮关闭 **P2-5（冒烟测试）** 与 **P0-2（puppeteer 缺失）**，提交 `f9b9f3a`（P2-5）、`99ddb3c`（P0-2）。

### P2-5 产物冒烟测试（v1.9.31）
- 新增 `tests/smoke.test.cjs`：校验产物存在性 / 体积 / UserScript 元数据 / 版本号（动态读 `src/version.js`）/ `vm` 语法编译。
- ⚠️ 顺序坑：`npm test` 必须 **build 先于 test:unit**，否则冒烟读到旧产物、版本断言失败。
  已改为 `lint → build → test:unit → validate`。测试总数 5 → 8。

### P0-2 批量采集（v1.9.32）
- 环境事实：`puppeteer-core@25.11.0` 已装；系统有 Chrome（`C:\Program Files\Google\Chrome\Application\chrome.exe`）与 Edge。
  故采用**方案 B**（puppeteer-core + 系统浏览器），无需下载 Chromium。
- 依赖：`package.json` 的 `puppeteer` → `puppeteer-core`。
- 新增 `src/lib/browser-resolver.js`：`loadPuppeteerCore()`（动态 `import()` + `/* turbopackIgnore: true */`）
  + `resolveBrowserExecutable()`（`PUPPETEER_EXECUTABLE_PATH` 优先，其次各平台常见路径）。
- `collector-core.js`：拆出依赖/浏览器加载，新增 `MISSING_BROWSER_MESSAGE`。
- 类型声明：`src/types/puppeteer.d.ts` → `puppeteer-core.d.ts`（补 `executablePath`）。
- **关键坑**：声明 `serverExternalPackages: ['puppeteer-core']` 会触发 Turbopack
  「Package puppeteer-core can't be external」（ESM 包被外部化）；**移除该声明**后 `next build` 告警 1 → 0。
- 实测：动态 import 加载 puppeteer-core 成功，驱动系统 Chrome 完成 `evaluate`（BROWSER SMOKE OK）。

### 验证与指标
- `npm test` 全绿（lint 0/0、build 92 模块 194.24 KB、8 用例、validate 通过）；`tsc --noEmit` 通过。
- `npm run build:web`：**0 告警**（原 1 条已消除）。
- `src/` 118 文件 / 8967 行；词典 459 条。
- 版本 1.9.32；CHANGELOG / PROGRESS / README / next.config / 各文档引用同步（puppeteer → puppeteer-core）。

### 状态：遗留任务清单 P0-1 ~ P2-7 **全部完成**。
