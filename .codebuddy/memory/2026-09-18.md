# 2026-09-18

## GitHub_Chinese 项目审查（v1.9.21）

用户要求"审查项目"。已完成静态审查（读核心模块 + 跑 build 验证 + 搜索一致性）。关键发现：

### 必须修复
- `src/core/errorHandler.js:125-126` 引用 `window.GitHub_i18n.translationCore.initDictionary()`，但全局从未定义 `window.GitHub_i18n`（main.js 只暴露 `window.translationCore`/`window.configUI`），字典超限自动重置失效（真 bug）。
- `src/versionChecker/fetcher.js:11-14` `KNOWN_SCRIPT_HASHES` 是占位哈希 `a1b2c3d4...`，完整性校验形同虚设（仅 warn 不阻断）。
- `src/main/lifecycle.js` `this.cleanup.bind(this)` 每次新建引用，removeEventListener 永远移除不了（逻辑错误）。
- `src/utils/securityUtils.js` 用已废弃 `escape`/`unescape`，应改 TextEncoder/TextDecoder。

### 质量/一致性
- `errorHandler.js:69-78` logError 两分支完全相同（死代码）。
- `config.js:43` externalTranslation 无引用（死配置/未实现）。
- `metadata.json` 声明 MAJOR_CAPABILITY_SERVER_SIDE_GEMINI_API，但代码纯本地词典无 AI，与 README 矛盾。
- `src/core/cacheManager.js` 满时全量 sort+clear+重建 Map，O(n log n) 性能隐患。

### 工程
- jest passWithNoTests:true 且无测试文件，覆盖率门禁从未生效。
- @eslint/js 未安装，lint 在干净环境失败（需 npm install）。
- 超 200 行源文件 4 个：i18n/manager.js(308)、core/virtualDom/manager.js(237)、dictionaries/common/misc.js(228)、translation-core/selectorUtils/patterns.js(221)。

### 验证基线
- build 成功：build/GitHub_i18n.user.js 76.83 KB / 2564 行。
- 版本号一致 v1.9.21（version.js/package.json/CHANGELOG）。
- 翻译注入用 createTextNode，XSS 风险低。

（待用户确认是否修复；未改代码）

## 重构 web/collector-guide.html（词典采集向导）

用户要求重构该页面（frontend-design 技能引导）。关键发现与改动：
- **根因**：原页面依赖 Tailwind 原子类，但项目 package.json 无 tailwindcss 依赖、无 web 构建脚本，`web/css/collector-guide.css` 仅 `@import "tailwindcss"` 无法展开 → 页面实际无样式。
- **方案**：改为自包含 CSS（`web/css/collector-guide.css` 重写，无外部字体/Tailwind 依赖，离线可用），主题对齐项目品牌绿 `#2ea44f`（GitHub 绿），去掉 SaaS 蓝、彩虹渐变进度条、ALL-CAPS 眉标、散布入场动画。系统字体栈。保留全部交互 id 契约（18 个）。
- **同步 JS**：renderer.js（预览行/备份徽章/tab 类）、utils.js（toast/copy 类）、stream.js（错误态类）注入的 Tailwind 类改为自包含类名。
- **版本**：全局 bump 1.9.21→1.9.22（package.json + src/version.js + CHANGELOG 小节 + web 文件头）。
- 验证：node --check 三个 JS 通过；grep 确认无残留 Tailwind 类；18 个 id 全部保留。
