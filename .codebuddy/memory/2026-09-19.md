# 2026-09-19

## web 采集向导 CSS 拆分（满足全局「源文件 ≤200 行」规则）
- 原 `web/css/collector-guide.css`（870 行）按职责拆分为 10 个模块，每个 ≤200 行：
  base / layout / sidebar / cards / buttons / code / terms / progress / terminal / toast。
- `base.css` 必须在前（定义 `:root` 设计变量、reset、滚动条、`@keyframes pulse`）。
- `collector-guide.html` 改为 10 个 `<link>` 引入；原 collector-guide.css 已删除。
- 验证：类名零缺失、JS 引用 id 零缺失、各模块括号平衡、无 @import（自包含离线可用）。
- 仍属 v1.9.22 未提交工作集（web 重构整体收尾），未二次 bump。

## 升级为 Next.js 16（src/ 模式）
- 采用 Next.js 推荐 src/ 结构：src/app、src/components、src/lib、src/hooks、src/config；新增 src/middleware.ts（Edge 安全头）。
- 根配置解耦：next.config.mjs、tailwind.config.ts、postcss.config.mjs、eslint.config.js（整合 eslint-config-next）、.husky/pre-commit 与 src/ 源码分离。
- 用户脚本核心（src/main.js 等 .js）保留独立构建，不纳入 Next。
- `next build` 通过；version bump 1.9.22 → 1.9.23（未提交工作集）。
- 注意：Next 16 build 提示 middleware→proxy 迁移（codemod 建议），middleware.ts 仍生效，暂保留。
