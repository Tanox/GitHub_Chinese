# 任务追踪（Task Tracker）

> 版本：**v1.9.48** ｜ 版本权威源：`src/version.js`
>
> 本文件是项目**任务清单**：仅列出未完成（活动）任务；已完成的任务记录于 `CHANGELOG.md`。
> `docs/PROGRESS.md` 仅作进度 / 架构 / 指标报告，不再重复维护任务表。

---

## 1. 活动任务（进行中 / 待办）

> 状态用 `- [ ]` 表示未完成；完成后在 `CHANGELOG.md` 对应版本小节记录。
> 优先级沿用 P0/P1/P2/P3；工作量标签：S（<0.5d）/ M（0.5–2d）/ L（>2d）。

**P1（成功率 / 覆盖率核心）**
- [ ] **T12** 采集提取精准化：从「整页 `body`」改为限定 GitHub UI 容器（react-app 根 / 已知布局容器），进一步降噪提升信噪比 `M`
- [ ] **T13** SPA/动态内容适配：等待 hydration 完成再提取；滚动触发懒加载；`networkidle2` 超时降级为 `domcontentloaded` + 固定等待 `M`
- [ ] **T14** 单次采集鲁棒性：逐 URL 错误隔离（单页失败不中断整批、记日志续跑）；导航超时/反爬（429）指数退避重试 `M`
- [ ] **T16** 匹配策略增强：模板串/占位符与复数归一、词级/子串模糊匹配，降低「已翻译却判待翻译」误报 `M`
- [ ] **T19** 词条级审阅工作流：每条待翻译词条标记 已翻译/忽略/需复核 并持久化 `M`
- [ ] **T20** 一键合并入库：将审阅通过词条按来源生成词典 stub + PR 式 diff 预览 `M`

**P2（度量 / 管理增强）**
- [ ] **T15** 采集并发上限与限流：复用浏览器实例 + 并发令牌（3–5）避免被封 `S`
- [ ] **T17** 覆盖率度量与报告：命中率指标 + 按页面/路由分类统计，输出低覆盖定位 `M`
- [ ] **T18** 采集源扩展：登录态 cookie 注入抓取私有页 / HAR 导入 `L`
- [ ] **T21** 翻译建议：对每条待翻译词条给出 LLM/翻译记忆建议译文，人工确认入库 `L`
- [ ] **T22** 覆盖率/缺口看板：按词典文件（nav/repo/pr/issue/misc…）展示覆盖率、Top-N 缺口、重复/冲突检测 `M`
- [ ] **T23** 历史明细与轮次对比：`collect-history.json` 扩展为词条级 diff + 回滚 `M`

**P3（体验打磨）**
- [ ] **T24** 导入/导出增强：CSV/JSON 双向 + 归一校验 `S`
- [ ] **T25** 搜索与批量操作：按状态/来源筛选 + 批量标记/忽略 `S`

---

## 2. 推进说明

- **单一来源**：项目任务以本文件为准；`docs/PROGRESS.md` 仅作进度 / 架构 / 指标报告。
- **流转规则**：活动任务完成后在 `CHANGELOG.md` 对应版本小节记录（历史任务 T1–T11、P0-1–P2-7 均已完成，详见 `CHANGELOG.md`；新增任务从 `T12` 起编号）。
- **新增任务**：按 `Txx` 编号追加到第 1 节对应优先级；文档准确性类问题优先以 T3 模式处理。
